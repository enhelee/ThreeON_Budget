# 변경이력 (CHANGELOG) — 실적분석 개선

> 업무 분담 협업용 변경 요약. 최신순. 상세는 각 문서/코드 참조.

---

## 2026-09-10 · Render 배포 디버깅 — Caddy 설정 파괴 버그 · 폴더명 ASCII화 (rev16)

**대상:** `deploy/Caddyfile`·`deploy/entrypoint.sh`, 패키지 폴더명. Render 첫 배포 2회 실패를 실측으로 규명.

### 실패 ① invalid Dockerfile path
`render.yaml`의 `dockerfilePath`가 `2026-09-09_팀공유(취합본)_v0/Dockerfile`(한글+괄호)일 때 Render가
경로를 인식하지 못함 → 폴더명을 `2026-09-09-team-package-v0`(영문·숫자·하이픈)로 전체 rename.
문서 6곳·`build_package.py`·루트 render.yaml 동시 갱신. 커밋 `15dc06f`.

### 실패 ② Failed deploy — entrypoint의 sed가 Caddyfile을 깨뜨림 (근본 원인)
`APP_PASSWORD`가 비었을 때 `sed -i '/basic_auth {/,/}/d'`로 인증 블록을 지우는 코드가 있었는데,
범위 종료 패턴 `/}/`가 **`{$BASIC_USER} {$BASIC_HASH}` 줄의 `}`에 먼저 걸려** 2줄만 지우고
닫는 중괄호를 고아로 남긴다. 결과: `handle /forecast* { 
 }` → Caddy 문법 오류
(`parsed 'handle' as a site address`) → 웹서버 기동 실패 → 헬스체크 실패 → 배포 실패.
Render에서 비밀번호를 아직 입력하지 않은 상태(sync:false)라 정확히 이 경로를 탔다.

**수정**: Caddyfile을 손대지 않는 **import 조각 방식**으로 교체.
- `Caddyfile`: `/forecast` 블록 안에서 `import /srv/deploy/forecast_auth.caddy`
- `entrypoint.sh`: 비밀번호 있으면 `basic_auth {...}`를 조각 파일로 기록, 없으면 **빈 파일** 생성
- 실측 검증(로컬 Caddy 2.8.4 `validate`): 비밀번호 있음 → Valid, 없음 → Valid,
  **옛 방식 재현 → Error**(원인 확정)

### 실측: Render 무료 플랜 용량 대비 우리 앱
Render 무료 = **0.1 CPU / 512MB**. 로컬 측정(가벼운 상태): forecast_app 240MB + budget_app 159MB
+ Caddy 25MB = **424MB / 512MB(83%)**. 분석 실행·Excel 내보내기·중장기 예측은 이보다 훨씬 더 쓰므로
무료 단일 컨테이너로는 실사용이 어렵다는 결론. Vercel Hobby는 **2GB / 1 vCPU / 300초**로 훨씬 여유롭지만
컨테이너가 **요청 단위·상태 없음**이어서 Streamlit(forecast_app)은 구조적으로 불가.

---

## 2026-09-09 · 배포 준비 — 인증·감사로그 · DB 전환 계층 · 모델 레지스트리 · 단일 이미지 · 소개 사이트 (rev15)

**대상:** `budget_app` + `deploy/` + `site/` + 패키지 `2026-09-09-team-package-v0/`. 테스트 **87 passed**(77 → +10). 문서: [배포가이드.md](배포가이드.md) · [보안설계.md](보안설계.md) · [학습데이터_모델_관리.md](학습데이터_모델_관리.md) · [팀원_실행가이드.md](팀원_실행가이드.md).

### 사용자 결정(질의 2회)
① 지금은 **무료 공개 배포**(Render Docker + Supabase PostgreSQL, 소개는 GitHub Pages; Vercel은 정적 사이트만 적합 — 서버리스 제한으로 앱 부적합), 최종은 **사내 서버** ② DB **SQLite 기본 + `.env`로 PostgreSQL 전환** ③ **공용 비밀번호 1개 + 수정 이력** ④ **한 주소 통합 + Docker Compose** ⑤ 데모가 아닌 **실데이터를 팀원만** 접근(사내 규정 확인 전제, 임시) ⑥ **모델 = DB 레지스트리(메타+BLOB)** ⑦ **학습데이터 = DB 테이블 + 확정 워크플로 + 수동 재학습**.

### 조치
- **인증·감사로그** `src/budget/auth.py`, `server.py AuthAuditMiddleware`: `APP_PASSWORD`(비면 인증 OFF) + **작업자 이름** → itsdangerous 서명 쿠키(HttpOnly·SameSite·Secure, `SESSION_HOURS`). `/api/*` 401 → 프론트 로그인 오버레이. 모든 POST/PUT/DELETE를 `audit_log`(시각·작업자·경로·상태·요약, 비밀번호 제외)에 기록, 실패 로그인 포함. API `/api/login|logout|auth/status|audit`, `/healthz`. 설정 탭 「변경 이력」.
- **DB 전환 계층** `src/budget/dbcore.py`: `DATABASE_URL=postgresql://…`면 psycopg 연결을 감싸 sqlite SQL을 번역(`?`→`%s`, AUTOINCREMENT→BIGSERIAL, INSERT OR REPLACE→ON CONFLICT(REPLACE_TABLES), PRAGMA table_info→information_schema, lastrowid→RETURNING id, numpy/NaN 파라미터 정리). `db.connect()`가 사용, `DATA_DIR/CONFIG_DIR/OUT_DIR` 환경변수. ⚠ PG 실서버 미검증(단위 테스트만) → `scripts/db_check.py`(접속 점검·`--migrate` SQLite→PG 복사).
- **모델 레지스트리·학습데이터** `src/budget/ml_registry.py`: `model_registry`(버전·메타·joblib BLOB·SHA256·활성·스냅샷)·`training_example`(확정 우선, 비활성화)·`training_snapshot`. v2 분류기 3종 이름 대응, 동일 파이프라인(TF-IDF char_wb+LogReg). API `/api/models*`, `/api/train`, `/api/training*`. 설정 탭 「분류 모델 레지스트리·학습데이터」(재학습·활성화/롤백·CSV 가져오기/내보내기·다운로드). `scripts/v2_model_sync.py --push/--pull/--status`(v2 파일 ↔ DB).
- **프론트** `webapp/index.html`: 로그인 오버레이(이름 필수)·작업자 배지·로그아웃, 설정 탭 2섹션 추가(컴파일 Tailwind 함정 회피 — 직접 CSS). template 동기화.
- **배포 구성** `deploy/`: 단일 이미지 Dockerfile(python:3.12-slim + Caddy + supervisord: uvicorn:8010 · streamlit:8501 `baseUrlPath=forecast` CWD=/data/forecast · caddy:$PORT), Caddyfile(`/`사이트·`/app` `/api`·`/forecast` basic_auth(bcrypt 기동 시 계산)·보안 헤더), docker-compose(볼륨·`--profile pg`), render.yaml, `.env.example`, `.dockerignore`, 통합 requirements(streamlit 1.59·pandas 2.2.3·psycopg 3). ⚠ 이 PC에 Docker 없어 이미지 빌드 미검증.
- **소개 사이트** `site/index.html`(패키지 `docs/index.html`, GitHub Pages·서버 `/`·FastAPI `/site`): 파이프라인·구조·검증 수치·계약·배포·보안·학습·역할·문서 지도. 라이트/다크 토큰, Gothic A1 + IBM Plex Sans KR/Mono.
- **패키지** `2026-09-09-team-package-v0/`(ASCII 폴더명 `budget_app/ forecast_app/ docs/ deploy/ matching_tool_src/`, 루트 Dockerfile·render.yaml·.env.example·.gitignore·실행기 3종·README). 199파일 5.3MB, 유출 검사 통과.
- 로컬 `budget_app/.env` 생성(gitignore) — 팀 공용 비밀번호 초기값은 바꿔 쓸 것. `requirements.txt`에 itsdangerous·python-dotenv·scikit-learn·joblib 추가(psycopg는 PG 사용 시).

### 검증
- pytest 87 passed: `test_dbcore.py`(번역 규칙 6) · `test_auth_registry.py`(401/로그인/감사로그, 재학습 v1→v2→롤백·다운로드·예측, 확정 우선, v2 파일 왕복 4). 브라우저: 로그인 오버레이 → 작업자 배지, 설정 탭 감사로그·레지스트리 표시, `/site/` 렌더.
- **PostgreSQL 실서버 검증(로컬 PG 16.4 바이너리, 2026-09-09)**: `db_check.py`로 18표 생성 → `--migrate`로 실데이터 13표 이전(16초) → `PG_TEST_URL`로 `test_auth_registry.py` 4건 통과 → 전체 API 워크로드(overview·branches·branch-detail·stats·export-team·analyze(run 4→6)·manual-biz·lock 423·learn·audit) 통과. **발견·수정**: PG `REAL`은 float4라 원 단위 금액이 −50/+144원 어긋남 → `dbcore` `REAL→DOUBLE PRECISION` 번역 추가 후 **SQLite와 원 단위 동일**(손익 98,554,800,525 · 자본 49,709,177,979). `db_check.py --migrate`는 대상 표를 비우고 전체 교체(`--source` 옵션 추가).
- **한 주소 라우팅 실증(Windows, Docker 없이)**: Caddy 2.8.4 :8080 → uvicorn :8010 + Streamlit :8501(baseUrlPath=forecast). `/` 200 · `/healthz` 200 · `/app/` 로그인→내보내기 200 · `/forecast` 401→team 인증 200. Dockerfile 빌드 단계만 미검증(Render 첫 빌드에서 확인).

---

## 2026-09-08 · 팀 v2 앱 병합 — 결과 JSON 내보내기로 3·4단계 개통 (rev14)

**대상:** `budget_app` + 루트 문서 + 팀 자료 반입. 테스트 **77 passed**(71 → +6). 상세: [비교분석_및_병합_보고서_2026-09-08.md](비교분석_및_병합_보고서_2026-09-08.md), 팀 진입점 [팀프로젝트_통합_README_2026-09-08.md](팀프로젝트_통합_README_2026-09-08.md).

### 배경
- 팀 GitHub(`enhelee/ThreeON_Budget`)에 동료 앱 스냅샷 `2026-09-08_예산예측프로그램`과 단독 실행형 `2026-09-08_팀공유_v2`가 올라옴. v2는 **3·4단계(표준화·중장기 26~35년)를 이미 구현**했고, 같은 날 커밋으로 「예산 실적 집계 → 사업 실적 연결」(결과 JSON schemaVersion 1 → 표준화·예측 입력)이 추가됨.
- 사용자가 올린 `docs/superpowers/예산 실적 집계`는 로컬 `budget_app`과 완전 동일(로컬이 최신).
- 사용자 결정: ① JSON 내보내기 + CSV 통합본 **둘 다** ② 3·4단계는 **팀 v2 기준선 채택**(우리 9/7 설계는 개선 제안으로) ③ v2를 루트 형제 폴더로 반입 + 전체 통합 업로드 패키지 ④ 커밋은 확인 후.

### 조치 ① 팀 연계 내보내기 (`spec_io` · `pipeline_db` · `server.py` · 프론트)
- `spec_io.build_team_result_json()` — 우리 매칭 결과(erp_annotated 손익+자본)를 v2 `checkpoint_actuals.prepare_result()`가 요구하는 JSON으로 변환. 묶음=(원장×지사×과목×귀속사업), 계획집행은 계획행 `_row`·신규는 합성 행번호(2,000,000+n)를 `집계표 원본행`으로, 미반영은 `exclusions`, 타예산(`''`)·제외행은 상대 원장 중복 방지로 제외. 불변식 Σ원천 = Σ배정 + Σ제외(미배정 0).
- `spec_io.write_matched_csv_combined()` — 연도 단일 `matched_{연도}.csv`(연동규격 §3-4, 손익+자본). `write_budget_csv_from_df`/`write_data_csv_from_df` — DB 로드본에서 직접.
- `pipeline_db.export_team_bundle()` — 손익·자본 최신 분석을 `make_files=False, record_run=False`로 재실행해 4종 파일(`팀연계_{y}_사업실적연결.json`, `matched/budget/data_{y}.csv`) 생성. **run 기록 없음**(rev13 재생성 함정 회피).
- `GET /api/export-team?year=&kind=json|matched|budget|data` — 손익·자본 어느 run이든 파일보다 새로우면 재생성.
- 「4 통계·내보내기」 상단 **팀 연계 패널**(버튼 4개), STAGE 5 안내문 교체. `body.template.html` 동기화.

### 조치 ② 정규화 일치
- `SEED_ITEM_ALIAS`에 `건가-외주비-열원정기점검 → 외주비-열원정기점검` 추가(동료 `builtin_categories.ACCOUNT_NAME_ALIASES`와 동일).

### 검증
| 연도 | 손익(원) | 자본(원) | 묶음 | 미배정 | v2 검증 |
|---|--:|--:|--:|--:|---|
| 2025 | 98,554,800,525 (=98,554,801천원) | 49,709,177,979 (=49,709,178천원) | 1,137 / 181 | 0 | 통과 |
| 2023 | 72,164,031,575 | 55,820,722,469 | 1,072 / 141 | 0 | 통과 |

- 동료 모듈 교차검증 테스트(`tests/test_team_bundle.py`, v2 폴더 있을 때만) — `prepare_result`·`compute_standard_amounts`까지 통과. 웹 `/api/export-team` 200·파일명 확인. v2 앱에 2023·2025 실제 연결(사업행 2,531·2,762.49억원 표시).
- v2 앱 자체 테스트 183건이 로컬 Python 3.14·pandas 3.0.3에서 전부 통과(원본 실행 bat은 3.12 전용 → 루트 `예산예측프로그램_v2_실행(로컬).bat` 추가).

### 문서
- 신규: `팀프로젝트_통합_README_2026-09-08.md`(팀 진입점) · `비교분석_및_병합_보고서_2026-09-08.md` · `연동규격_INTERFACE.md §8`(결과 JSON 계약).
- 현행화 블록: 연동규격/통합가이드/필드매핑(7/22 미해결 4건 결말), 설계_3-4단계(팀 v2 기준선·개선 제안으로 전환), 연계규격 §2.6·2.7(보류), MASTER·HANDOFF·README_실행방법.
- 반입: `예산예측프로그램_팀공유_v2/`(코드 무수정) · `팀자료_2026-09-08/` · `PRD_팀현행_V1비교_2026-09-08.html` · GitHub 재업로드용 `업로드용_ThreeON_Budget_2026-09-08_팀통합/`.

---

## 2026-08-04 · 저장 / 분석 / AI학습 3분리 — 수정 대기시간 제거 (rev13)

**문제(사용자):** 고칠 게 많은데 **한 건 수정할 때마다 손익·자본 전체 재분석**이 돌아 매번 30초씩 기다렸다.

### 원인 — 프로파일링으로 확인 (추측 아님)
2025년 손익 분석 1회 13.8초의 내역:

| 단계 | 시간 | |
|---|---|---|
| 원자료·지시·마스터 로드 | 0.1s | |
| 처지사 정규화 | 0.7s | 엔진 |
| 사업 매칭 | 3.9s | 엔진 |
| 스코프·요약·집계 | 0.7s | 엔진 |
| 실적.xlsx | 0.6s | 산출물 |
| **zrfm2_V1.xlsx** | **8.2s** | 산출물 ← 최대 병목 |
| matched.csv | 0.2s | 산출물 |
| DB 기록(match_line 14,290행) | 1.5s | |

→ **대기시간의 63%가 아무도 지금 열어보지 않는 Excel 파일 생성.**

### 조치 ① 산출물 파일을 분석에서 분리
- `run_actual_frames(make_files=False)` / `run_actual_db(make_files=False)` 추가. `/api/analyze`는 파일을 만들지 않는다.
- `/api/export`가 **필요할 때 생성**한다(파일이 없거나 최신 run보다 오래됐을 때만).
- **함정 하나 잡음**: 파일 생성이 새 run을 기록하면 방금 만든 파일이 곧바로 'run보다 오래됨'이 되어 **매 다운로드마다 재생성**된다 → `record_run=False` 추가. 검증: 첫 다운로드 34초(생성), 이후 **0.23초**(캐시).

### 조치 ② 저장 / 분석 반영 분리 (사용자 확정)
- 재배정·사업수정·삭제·사업추가 저장에서 **자동 재분석을 제거**. 저장은 서버 기준 **0.04초**.
- 헤더 분석 버튼이 미반영 변경이 있으면 **`분석 반영 (N)`** 으로 바뀌고 강조(주황+펄스), 상세 화면 상단에 **미반영 바**(무엇이 몇 건인지) 표시.
- `GET /api/pending?year=` — 최신 run 시각 이후 저장된 지시(재배정/수정/삭제/추가) 건수 + 학습 대기 건수.
- **분석 범위는 항상 손익+자본**(사용자 확정) — 손익·자본을 섞어 수정해도 헛가릴 여지가 없게.

### 조치 ③ AI 학습 분리 (사용자 확정)
- `/api/override` 저장 시 자동 학습(`learn_from_override`) **제거**. 학습은 설정 탭 **[AI 학습]** 버튼에서만.
- `db.count_learn_pending` 추가 → 버튼 라벨에 `(대기 N건)`, 미반영이 있으면 "먼저 분석 반영" 경고.
- 부수 효과로 **안전해짐**: 종전에는 잘못 재배정하고 되돌려도 학습에 흔적이 남았다. 이제 안 남는다.
- 권장 순서를 화면에 명시: ① 수정·저장 → ② **분석 반영** → ③ **AI 학습** → ④ 연도 마감.
- 학습의 성질(사용자 질문에 대한 확인): 저장 순간에는 아무것도 재분류되지 않는다. 그러나 **다음 분석에서 같은 (예산과목 + 정규화된 전표텍스트)를 가진 다른 전표도** 그 사업명으로 귀속된다(학습표는 전 연도 공용). 안전장치는 ⓐ 사람이 직접 재배정한 전표는 확신도 1.0으로 학습 0.99보다 우선 ⓑ 그 (과목×지사)에 같은 이름의 사업이 없으면 미적용 ⓒ 마감 연도는 재분석 차단.

### 효과 (실측)
| | 전 | 후 |
|---|---|---|
| 수정 1건 저장 | ~30초 | **0.04초**(서버) / 1.6초(화면 재조회 포함) |
| 분석 반영 | 수정마다 강제 | 원할 때 1회 **16~19초** |
| 첫 내보내기 | 0.2초(미리 생성돼 있음) | 34초(생성) → 이후 0.23초 |
| **수정 10건 작업** | **약 5분** | **약 20초** |

### 자체 검증(실데이터, 되돌림 완료)
UI로 수정 저장 → 미반영 배지 1·분석 미발생 확인 → 설정 탭 이력 삭제로 원복 → 분석 1회 →
**손익 98,554,802 / 자본 49,709,177**(검증 기준치와 정확히 일치)·미반영 0·테스트 값 잔존 0 확인.
테스트 3건 추가(파일 미생성 시 숫자 동일 / 내보내기 재생성이 run을 늘리지 않음 / 저장 시 학습 안 됨).

---

## 2026-08-04 · 사업 이동·삭제·수정 확장 + 표·버튼 가시성 (rev12)

**대상:** `budget_app` 웹(상세 화면 중심) + 엔진. 테스트 **68 passed**.
커밋 `d6ea180`(웹 전환 이후 미커밋분 일괄 편입 포함) → `ac83c7e` → `9bc6985` → `914714a` → `397dcfa` → `a0fa514` → `31de98a`.

### ① 사업 통째 지사 이동 (사용자 요구: "삭제·재생성 없이 옮기고 싶다")
- 오해의 원인: ✎ 폼의 **주관부서명은 표기용 텍스트**이고, 종합표·지사별 집계 기준은 **처지사**다.
- 이동은 **기존 저장소 두 개를 동시에** 쓴다 — 계획행(연예산)=`biz_edit.fields.처지사`, 귀속 전표(실적)=`override.target_dept`. **둘 다 매칭 전에 적용**되므로 새 지사에서 다시 만나 `계획집행` 관계·확신도가 유지되고 총액 보존식은 불변.
- ✎ 폼 맨 앞에 **예산귀속 지사(처.지사)** select. 지사를 바꿔 저장하면 확인창 → 그 사업의 전표를 **자동 전수 수집**해 함께 이동 → 자동 재분석 → 화면이 새 지사로 전환. 전표를 손으로 고를 필요 없음.
- `db.patch_latest_run_biz`가 지사 변경 시 `match_line.처지사정규`도 함께 갱신(전표 트리 정합).
- 설계: `budget_app/docs/superpowers/specs/2026-08-04-사업-지사이동-design.md`

### ② 중복 사업 행 근본원인 제거 (개명 시 참조 동기화)
- 사용자 사례: `＋사업 추가`로 만든 사업에 전표를 재배정한 뒤 **이름만 수정**(`(광양항태양광)`→`(광양항 태양광)`) → 재분석에서 **동명 신규 사업이 따로 생김**.
- 원인: `override.target_name`·`learned_match.사업명`이 **옛 이름을 계속 가리켜** 그 이름의 계획행이 없으니 강제 신규가 됨.
- 조치: `db.rename_biz_references` — 개명 시 override·learned를 함께 이동(override는 과목·지사 컬럼이 없어 **최신 run의 match_line으로 범위를 좁힘**). 저장 알림에 연동 건수 표시.
- **manual_biz는 일부러 건드리지 않음** — 원본 이름을 바꾸면 그 `biz_edit`(옛 이름 키)이 어긋나 속성 등 나머지 수정까지 사라진다.

### ③ 사업 삭제 + 전표 합치기 (`biz_delete`)
- 새 테이블 `biz_delete` = **분석 대상에서 제외** 기록. `plan_row` 원본은 보존 → 설정 탭 **「사업 삭제 이력」 → 되돌리기**로 복구. 검토리포트 `계획행제외`에 "관리자 사업 삭제"로 공시(조용한 누락 금지).
- 귀속 전표가 있으면 **옮길 곳을 반드시 선택**(같은 지사·같은 과목의 다른 사업 또는 `"{지사} {과목} 미지정"`) → override + 삭제를 한 번에. 전표를 지우지 않으므로 총액 보존식 유지.
- **계획행이 없는 행(신규·미매핑)은 삭제 기록을 남기지 않는다** — 삭제 키가 이름 기준이라 **동명 계획행까지 지워지는 결함**을 발견해 차단. 전표 이동만 수행하고, 전표가 떠나면 그 행은 저절로 사라진다.
- 이동 대상 목록은 **자기 자신만 행 단위(`_key`)로 제외** → 동명 쌍둥이를 골라 중복을 하나로 합칠 수 있다.

### ④ 연예산(A)·예산과목 수정
- `biz_edit`에 **연예산**(천원, 서버·클라 숫자 검증, 빈칸=결측 복귀)과 **예산과목** 허용.
- **원본 `plan_row`는 보존하고 매칭 전 계획행에만 덧씌우는 방식** — 이후 모든 산출물(양식1·종합표·집행률·통계·export)이 같은 값을 쓰고, 이력 삭제로 원복된다. 원본 직접 수정은 **채택하지 않음**(승인 계획본과의 대조 기준선이 무너지고 되돌릴 수단이 없어짐).
- 예산과목은 **같은 예산구분(손익/자본)에 등록된 과목만** select에 노출. 다른 예산 과목으로 옮기면 `biz_edit`이 그 budget run에만 로드되므로 **양쪽 run에서 '타예산 과목'으로 제외돼 행이 사라진다** → 원천 차단.
- 전표의 과목은 **ERP 계정코드**가 정하므로 과목 변경 시 **귀속 전표는 옛 과목에 신규로 남는다**(확인창·안내 문구로 사전 공지). 즉 이 기능은 계획본 분류 오류 정정용.
- 사업명·연예산·지사·과목 변경은 매칭/집계 키라 **저장 후 자동 재분석**(속성·부서명은 즉시 패치만).

### ⑤ 재배정 바 개선
- **이동할 사업 선택이 '이동할 지사' 기준**으로 바뀜 — 지사를 바꾸면 그 지사의 사업 목록을 불러와(연도·지사별 캐시 `state.ovBiz`) **타 지사·처의 기존 사업에 직접 붙일 수 있다**. 라벨에 기준 지사·건수 표시.
- 첫 항목을 **`— 선택 없음 (직접 입력) —`** 으로 두고 기본값으로 삼음 — 종전에는 목록 첫 사업이 늘 선택돼 **엉뚱한 사업에 붙을 위험**이 있었다.
- 재배정 바 입력 3개(지사·사업 선택·직접 입력)를 `state.det.ov*`에 보관 — `#detResults`가 전표 체크·검색마다 재렌더돼 입력이 날아가던 문제 해결(**일부 전표만 골라 옮길 때 필수**).
- 미매핑 가상지사 상세에서는 `— 지사 선택 —`으로 시작(종전엔 첫 지사 묵시 선택), 미선택 저장 차단.

### ⑥ 표·버튼 가시성 (실측 기반)
- **⚠ 이 페이지의 `<style>`은 프로토타입에서 컴파일된 Tailwind 서브셋이고 런타임 Tailwind가 없다** — 원본에 없던 유틸리티 클래스는 **아무 효과 없이 무시된다**. 실제 사고: 삭제 버튼에 `bg-red-600 text-white` → 배경 투명 + 흰 글자 = 안 보임. 미컴파일 확인: `bg-red-*`, `border-red-200`, `text-red-700/800`, `lg:grid-cols-5`, `ml-auto`, `max-w-xl`.
  → `.btn-danger`(#dc2626+흰 글자, 대비 4.8:1), `.btn-danger-outline`, `.danger-box/-title/-text/-label`, `.edit-grid`, `.push-right`, `.max-w-30`을 **직접 CSS로 정의**. 새 스타일은 항상 `getComputedStyle`로 적용 여부를 확인할 것.
- 상세 표 열 폭을 브라우저에서 **실측**해 재배분(폰트 12.16px·셀 패딩 11.2px 기준: 예산과목 150px·전표번호 67px·금액 85px·상태 배지 77px·확신도 50px·헤더 '구분 ↕' 61px)
  → 구분 4rem · 예산과목 10.8rem · 전표번호 5.2rem · 금액 6.2rem×2 · 상태 5.4rem · 확신도 4.4rem · 수정 2.4rem, 표 `min-width:56rem`.
  긴 값은 `…`로 자르지 않고 **줄바꿈**(`overflow-wrap:anywhere`), 금액·날짜·전표번호는 nowrap.
  검증: 1071px·1265px 창에서 **예산과목 두 줄 0건 · 잘린 셀 0 · 가로 스크롤 없음**(1009px에서도 사업명 202px 확보).
- ✎ 폼은 7항목(지사·과목·사업명·속성·주관부서명·부서(부)·연예산) → 1→2→**4열**(4+3 두 줄). 7열로 몰면 select이 너무 좁아짐.
- 전표 트리 헤더의 '전체 선택'은 **글자만 제거하고 체크박스 기능은 유지**(라벨='예산과목', `data-vall` 핸들러 존속).

### 테스트 (68 passed)
신규 8건: 지사 이동 즉시패치 / 사업 통째 이동(새 지사 계획집행·연예산·실적 동반, 구 지사 소멸, 총액 보존) / 삭제·복구 / 전표 이동 후 삭제 총액보존 / 개명 동기화 후 **중복 없음** / 연예산 수정·복구 / 예산과목 변경(계획행·연예산 이동 + 전표 총액 보존).

---

## 2026-08-04 · 통합 인수인계 패키지 (rev11)

**목적:** 다른 컴퓨터에서 Claude Code로 분석·통합할 수 있는 완전한 이관 자료 작성.

- **`통합_인수인계_MASTER.md`(루트)** ★ 진입점 — 이관 체크리스트, 디렉토리 구조, 아키텍처, 분석 로직(R1~R12+학습+마감), DB 스키마·데이터 요약, API 명세 전체, 프론트 빌드법, 검증 기준치, 주의사항 8건, 로드맵, 문서 지도.
- **`budget_app/docs/DB_스냅샷.md`** — budget.db 전체 문서화(스키마·데이터셋·최신 run 요약·오버라이드 69건 전체·학습 수동확정 목록·마스터·검증 기준치). `scripts/make_db_snapshot.py`로 재생성 가능.
- **`scripts/db_정리.py`** — 과거 run/match_line/biz_line 정리(+VACUUM, 원본·확정 데이터 무손실) — 이관 전 용량 축소용(121MB→약 20MB).
- `requirements.txt`에 fastapi/uvicorn/python-multipart 추가(신규 PC 설치 대비), `README_실행방법.md` 웹 시스템 기준으로 전면 갱신.
- 이관 핵심: **`budget_app/` 폴더(특히 data/budget.db) + 루트 md 문서**만 복사하면 완전 재현.

---

## 2026-08-04 · 한글검색 IME 픽스·사업 추가·전표 TO-BE 컬럼 (rev10)

**대상:** `budget_app` 웹. 테스트 **61 passed** + 23·25년 실데이터 브라우저 검증.

### 한글 검색 조합 끊김(IME) 수정
- 원인: 검색 입력마다 화면 전체 재렌더 → 입력창 DOM이 교체되며 한글 조합이 낱자(ㅅㅜㅈㅣㄹ)로 깨짐.
- 수정: 사업명·지사명 검색은 **결과 영역(#detResults/#brResults)만 부분 갱신** — 입력창 DOM 유지로 조합 정상. (브라우저 검증: input 요소 identity 유지 확인)

### ＋ 사업 추가 (사용자 요구)
- 상세 화면 필터줄에 **'＋ 사업 추가'** 버튼 → 폼(예산구분/예산과목(구성 select)/속성(비우면 마스터 자동)/연예산(천원)/사업명/주관부서명/부서(부)). 저장 즉시 자동 재분석.
- 구현: `manual_biz` 테이블 → 분석 시 계획행으로 편입(_row=1,000,000+id 합성키, 전표트리 조인 안전). 실적 없으면 '미시행'으로 표시되고, **전표 재배정 대상 사업**으로 사용 가능. 설정 탭에 이력·삭제(다음 분석부터 제외). 마감 연도 차단.
- **주의**: 현재 헤더에서 선택된 연도로 저장됨 — 연도 확인 후 추가할 것.

### 전표 트리 TO-BE 컬럼화 (사용자 시안 반영, 23·25년 공통)
- 구분표/전표행을 컬럼형으로 재배치: **[전체선택] 예산과목 | 전표번호 | 전표 텍스트(ERP) | 전기일 | 전표금액(원) | 확신도**. branch-detail 전표 payload에 `과목`(과목정규) 추가. 고정 열폭 조정(과목 7.8rem·전표번호 5.2rem) — 1280px에서도 한 화면 유지.

---

## 2026-08-04 · 미매핑 노출·타지사 이동·마스터 UI·마감 스냅샷·연도 비교 (rev9)

**대상:** `budget_app`. 테스트 **60 passed** + 25년 실데이터 미매핑 이관 왕복 검증.

### 미매핑 전표 노출 + 타지사 이동 (사용자 요구)
- **미매핑 가상 지사**: 처지사 미확정 전표를 원문 지사별 `"{원문} (미매핑)"` 행으로 전지사 목록 **맨 아래** 노출(빨간 배지 '미매핑 N건·금액', 종합표 미포함 명시). 상세로 들어가면 과목별 `[미매핑]` 사업 행(회색) + 전표 트리.
- **타지사 이동(크로스 재배정)**: 재배정 바에 **'이동할 지사'** 선택 추가 — A지사 전표를 B지사/C처의 사업으로 이동 가능(계획 사업명 정확 일치 시 그 사업, 아니면 그 지사의 신규). `override.target_dept` 컬럼(마이그레이션), matching이 매칭 전에 처지사를 바꿔치기. 검증: 본사 미매핑 전표 1건(17,520천원) → 플랜트기술처 이관, 미반영 188,786→171,266, 총액 보존 정확.
- 범례에 회색=미매핑 추가.

### 미매핑 실전 재배정 검증 (25년, 웹 UI 실제 조작)
- **전표 전체 선택** 기능 추가(전표 구분표에 체크박스 — 27건을 한 번에 선택, 개별 클릭 불필요).
- 25년 본사 미매핑 30건을 웹 화면에서 실제 이관: 건물/구축물 27건→플랜트기술처 「본사 사옥 건물·구축물 수선(미매핑 정리)」(186,842천원), 지급수수료 3건→「본사 열원점검수수료(미매핑 정리)」(1,944천원).
- 검증 결과: **미매핑·미반영 0**, 손익 98,554,802+자본 49,709,178=ERP 전체(+1 반올림), 전지사 합계 일치, 이관 사업 전표합 정합, Sheet1=종합표 SUMIFS 차이 0, 자동 학습 22건(텍스트 없는 8건은 학습 제외 정상). **재배정은 설정 탭 이력에서 삭제(원복) 가능.**
- **23년도 동일 처리 완료**(사용자 지시): 건물/구축물 31건/314,252천원 + 지급수수료 8건/846천원 → 플랜트기술처 동일 사업명. 결과: 23년 미매핑·미반영 0, 손익 72,164,026 + 자본 55,820,726 = ERP 전체(-2 반올림), Sheet1=종합표 차이 0, 학습 누적 42건.
- **23·25년 일괄 학습 완료**(사용자 지시): 학습 DB 총 **10,888건**(사람확정 42 + 자동확정 10,846) — 이후 모든 연도 분석에 자동 적용. 연도 마감(잠금)은 사용자 수정분석 예정으로 보류.

### 나머지 제안 4종 (사용자 승인)
1. **마스터 업로드 UI**: 설정 탭 '파일 갱신' 버튼(예산과목/부서코드) → `/api/upload/master` (같은 코드는 덮어씀).
2. **부서코드→처지사 보정**: 계획행 처지사가 지사구성에 없으면 부서코드 마스터로 채움(매핑 결과가 등록 지사일 때만).
3. **마감 스냅샷**: 연도 마감 시 산출물 6종(실적×2·V1×2·matched×2)을 `output/마감/{연도}/`에 자동 보관.
4. **연도 비교 통계**: 통계 탭에 '전년 대비 증감'(과목별 전년실적/당년실적/증감/증감률, 직전 분석 연도 자동 선택) — 23↔25 표시 확인.

---

## 2026-08-04 · 마스터 DB화·속성 교정·상세 화면 개선·연도 마감 (rev8)

**대상:** `budget_app`. 테스트 **59 passed**. 사용자 제공 마스터 2종 DB화.

### 기준정보 마스터 (사용자 제공 파일 DB화)
- `예산과목.xlsx`(457건: 계정코드·과목명·속성) → **item_master**, `부서코드.xlsx`(83건: 부서코드·부서명·처지사) → **dept_master**. 설정 탭에 현황 표시.
- **속성 4종 교정**: 계획 속성이 일반/제조/건가/자산이 아니면(25년 자본 원본이 속성 칸에 과목명을 넣은 품질 문제) 마스터(과목→속성)로 교정. 신규 행 속성도 마스터로 채움. 상세 속성 필터 = 4종 고정 순서.
- item_master의 계정코드→과목명이 ERP 과목 정규화에도 자동 사용(DB 모드 마스터 없던 공백 해소).

### 실적분석(상세) 화면 개선 (사용자 요구)
- **한 화면 표시**: 고정 레이아웃 컴팩트 테이블(1280px 뷰포트에서 가로 스크롤 0), 사업명·과목 말줄임+툴팁.
- **정렬 화살표 1줄**: 헤더 nowrap, 화살표가 문구 옆.
- **원 단위**: 표의 연예산(A)·실적금액(B)·전표금액 = **원 단위**(실적원=귀속 전표 원단위 정밀 합). 상단 요약 카드 = 천원 + 작은 '천원' 표기.
- **전표 구분표**: 사업 행 펼침 시 전표번호/전기일·텍스트/전표금액(원)/확신도 서브헤더 표시.
- **범례**: 표 상단에 색상 의미(흰=계획집행, 분홍=미시행, 노랑=신규) + 확신도 기준 안내.

### 연도 마감(잠금) — 완료 연도 보호 (사용자 요구)
- **year_lock**: 설정 탭에서 연도 마감 → 업로드·분석·재배정·수정 **HTTP 423 차단**(조회·통계·export는 허용). 이후 학습이 업그레이드돼도 마감 연도 숫자는 절대 불변. 마감 연도는 상세·업로드 화면에 🔒 배너, 수정 UI 숨김. 해제 가능.

### 학습 구조 명확화
- 학습 DB는 **연도 구분 없이 전체 연도 공용 1개** — 어느 연도에서 학습해도 모든 연도 분석에 적용(2단계 불필요). 설정 탭 문구·버튼명 명확화("확정 결과를 전체 학습 DB에 추가").

---

## 2026-08-04 · 웹 고도화 — 학습·사업수정·다년도·자체검증 (rev7)

**대상:** `budget_app` 웹(server.py + webapp). 테스트 **56 passed** + 23년 실데이터 자체검증 11항목 전 통과.

### 사용자 요구 반영
1. **미시행 숨기기 토글** — 상세 탭 필터(기본은 전체 표시).
2. **재배정 저장 즉시 자동 재분석** — 저장 → 재분석 → 최신 결과로 화면 자동 갱신.
3. **홈 = 다년도 현황** — 분석 완료된 모든 연도의 계획/실적 막대 + 선택연도 손익/자본 카드(계획·현재까지 집행률). 연도 클릭으로 전환.
4. **연도 선택 일반화** — 헤더 연도 select + '＋연도' 버튼, 계획/실적집계 탭에 연도 선택기 + **새 연도 직접 입력**. 페이지에 박힌 연도 제거, 데이터셋 목록은 선택 연도의 최신본에 '사용중' 배지.
5. **상세 탭 컬럼 정렬** — 구분/예산과목/속성/사업명/연예산/실적/상태/확신도 헤더 클릭(▲/▼/기본 3단 토글).
6. **🧠 확정 결과 학습(설정 탭)** — `learned_match` 테이블: 검토 완료 연도의 확정 매칭(전표 텍스트정규→사업명)을 축적, **사람 확정(재배정)이 자동 확정보다 우선**. 다음 분석부터 유사도보다 학습 맵을 우선 적용(확신도 0.99 '학습' 배지). 23년 검증: 학습 5,036건 → 재분석 시 8,659건 전표 학습확정, 총액 보존. 학습 초기화 버튼 포함.
7. **사업 내용 수정(✎)** — 상세 행의 ✎ → 사업명/속성/주관부서명/부서(부) 인라인 편집(빈칸 저장=값 지움). `biz_edit` 테이블에 영구 저장: 즉시 화면 패치 + 이후 재분석에도 매칭 전 계획행에 적용. 설정 탭에 수정 이력·삭제.

### 자체검증에서 발견·수정한 결함
- **동명 사업 전표트리 오염**: 같은 (과목×지사)에 같은 사업명이 2행이면(광주전남 '유해화학물질 취급시설') 미시행 행에도 남의 전표가 표시 → 매칭 결과에 **귀속 계획행 번호(매칭행)** 를 기록(match_line·biz_line.src_row, 기존 DB 자동 마이그레이션)하고 전표 트리를 행 단위로 조인. 자체검증 4번 항목(전 지사 × 전 사업: 사업 실적 = 귀속 전표 합) 불일치 0건.

### 재배정 저장 즉시 자동 학습 (사용자 확정, rev7 추가)
- 상세 화면에서 **재배정 저장 → 그 전표의 텍스트→사업명 매핑이 즉시 학습 DB에 자동 축적**(`db.learn_from_override`, source='수동확정'). 별도 버튼 불필요. 텍스트 없는 전표는 학습 대상 아님(정상 스킵).
- 설정 탭 '확정 결과 학습' 버튼 = 검토 끝난 연도의 **자동 매칭까지 통째로** 일괄 학습하는 용도(재배정 자동 학습과 별개). 테스트 58 passed.

### 본사 전표 기본 귀속 규칙 (사용자 확정, rev7 추가)
- **I열='본　　사' + J열(부서명) 없음 → 플랜트기술처** 기본 귀속(`matching.normalize_erp_df` ④단계). J열에 부서명이 있는 본사 전표(경영지원처 총무부 등)는 종전대로 미매핑 보고 — 열원 무관 비용의 과대계상 방지.
- 결과: 23년 자본 55,820,726 / 25년 자본 49,709,178 — **종전 검증치와 정확 일치, 자본 미반영 0**. 손익 미반영은 J열 있는 본사 전표만 잔존(23년 315,098 / 25년 188,786). 총액보존 유지, 플랜트기술처 트리 정합 0건 불일치. 테스트 57 passed.

### 25년 자료 검증 (웹 업로드 → 분석, rev7 추가 검증)
- 업로드: 계획 1,700행(손익·자본 혼재 원본) / zrfm2 14,290행. 계획 스코프 분리 정확: 손익 103,384,950 + 자본 61,383,488 + 건설공사(실적반영 해제) 8,484,032 = 원본 173,252,470 정확 일치.
- 총액보존: 손익 98,366,016 + 자본 49,320,475 + 미반영 577,489 = **148,263,980 ≒ ERP 전체(+1천원 반올림)**.
- 종전 검증치 재현: 손익 98,366,016(≒98,554,801−미반영 188,786) · 자본 49,320,475+미반영 388,703 = **49,709,178 정확 일치**.
- 자본 미반영 388,703천원 = **본사 전표 36건(J열 부서명 없음)**: 공구와기구 323,190 / 구축물 47,617 / 기계장치 17,896 — 검토리포트에 상세 보고. 수기 분석본은 이를 플랜트기술처 등으로 수기 배정했었음 → 필요 시 상세 화면 재배정 또는 '본사 기본 귀속 지사' 설정으로 처리 가능.
- 전 지사(24개) 전표트리 정합: 사업 1,779건 검사 **불일치 0건**. 통계·export 6종 정상. 홈 다년도 화면에 23·25 동시 표시.

### 자체검증(23년 실데이터, API E2E 11항목)
업로드(1302/9850행) → 분석 총액보존(-1천원) → 전지사 합계 일치 → **전 지사 전표트리 정합 0건 불일치** → 재배정 왕복(총액차 0) → 사업수정 즉시패치+재분석 유지 → 학습 적용 → 통계 합계 일치 → 내보내기 6종 200. 검증 스크립트로 재실행 가능.

---

## 2026-08-04 · 새 웹 기반 전환 — FastAPI + 프로토타입 UI (rev6)

**대상:** `budget_app`. 테스트 **54 passed**. Streamlit UI는 유지(레거시), **기본 실행은 새 웹**.

### 아키텍처 (Streamlit → FastAPI + SPA)
- **`server.py`** — FastAPI 백엔드. 업로드(계획/zrfm2 → DB 흡수, 원본 미보관) · 분석 실행(손익+자본 일괄) · 지사 현황/상세 · 재배정 · 통계 · Excel export API. 프론트는 정적 `webapp/index.html` 서빙.
- **`webapp/index.html`** — `budget-analysis-prototype.html` 디자인(사이드바 SPA, Tailwind 컴파일 CSS)을 그대로 계승. 메뉴: 홈 / 1 예산 계획(계획 업로드) / 2 실적 집계(zrfm2 업로드+분석) / 3 실적 분석(전지사) / **3+ 실적분석(상세)** / 4 통계·내보내기 / 5 중장기 예측(예정) / 설정.
- `db.py`에 **biz_line**(사업 단위 결과) 테이블 추가 — run별 계획행+신규행 저장(웹 상세화면 소스).
- 실행: `분석프로그램_실행.bat` = `uvicorn server:app --port 8010` (launch.json `budget_web`).

### 실적분석(상세) — 사용자 스크린샷·PPT 슬라이드5 구현
- 지사 선택 → **손익·자본 통합** 전체 사업 목록(예산과목·속성·사업명·연예산(A)·최종 실적금액(B)·상태·확신도).
- 필터: 예산구분 / 예산과목 / 속성 / **사업명 검색**.
- 사업 행 클릭 → **귀속 ERP 전표 트리**(전표번호·전기일·텍스트·금액·확신도, 상쇄± 포함).
- 전표 체크 → 다른 사업 선택 또는 **신규 사업명 직접 입력** → 재배정 저장(DB override) → '분석 실행' 재실행 시 반영. 신규=노랑·미시행=붉은 배경.

### 통계·내보내기
- 손익/자본 **과목별 계획 대비 실적**(대분류·계획집행/신규 분해·집행률·미시행) + **지사그룹별 막대**(25년 확정 분류체계 시드).
- Excel export: `{연도}년 {구분}예산_실적.xlsx` · `zrfm2_{연도}_V1({구분}).xlsx` · `matched CSV` 다운로드 버튼.

### 엔진 소폭 개선
- 텍스트 없는 전표끼리 (과목×지사) 내 **한 클러스터로 병합** — "(사업명 없음)" 행 난립·상쇄쌍 분산 방지 (강남 121→112행).
- 상세 목록 정렬: 과목 → 계획 사업 먼저 → 실적 큰 순.

### E2E 검증 (23년, 브라우저 실동작)
- 홈 집계 = 엔진 결과 일치(손익 71,848,928 / 자본 55,749,751천원). 상세 트리에서 전표 1건(524,105천원) 재배정 → 재분석 → 지정 사업명으로 이동 확인 → 테스트 흔적 삭제·원복. export 4종 200 OK.

---

## 2026-08-04 · 구조도(PPT) 반영 — 전표번호 묶음·보안 DB·검토 UI (rev5)

**대상:** `budget_app`. 테스트 **54 passed**. 근거: `중장기 예산 소요 전망 구조도.pptx`.

### 매칭 엔진 (PPT 슬라이드 4~8 핵심 규칙 구현)
1. **참조전표번호 묶음(R6).** 같은 참조전표번호 = 하나의 내역. 묶음 내 |금액| 최대 전표의 텍스트를 대표 사업명으로 매칭 → 인지세 등 부대비용 전표가 본 대금 전표를 따라감(슬라이드 6·8).
2. **신규사업 텍스트 클러스터링(R10).** 귀속 안 된 묶음을 대표 텍스트 유사도 ≥80로 묶어 사업 단위 행 생성. 표기가 다른 월별·상쇄(±) 전표가 한 사업으로 묶여 순액 집계(슬라이드 7). 23년 손익 신규 412행 → **188행**. 소액 판정은 클러스터 순액 기준으로 변경.
3. **수동 재배정(오버라이드, R12).** `match_actuals(..., overrides=)` — 전표묶음을 지정 사업으로 이동(계획 사업이면 그 행, 아니면 그 이름의 신규 강제, 확신도 1.0).

### 보안 아키텍처 (백엔드 DB)
- **`src/budget/db.py`** — SQLite(`data/budget.db`, gitignore). 업로드 파일을 행 단위로 DB에 흡수하고 **원본 파일은 즉시 삭제**. dataset/plan_row/erp_row/override/run/match_line 테이블. 분석은 DB에서 실행(`pipeline_db.run_actual_db`), 실행 이력·매칭 상세·수동 재배정이 모두 DB에 기록.
- `pipeline_actual`을 `run_actual`(파일) + `run_actual_frames`(DataFrame 공용 코어)로 분리. DB 모드용 `write_zrfm2_v1_from_df` 추가(원본 파일 없이 V1 생성).

### UI — 앱 3번째 탭 「③ DB 분석·검토 (보안)」
- ① 자료 등록(DB 흡수) → ② 분석 실행 → ③ **사업 트리 검토**(과목·지사 필터 → 사업별 expander → 귀속 전표 목록 + 확신도, 낮은 확신도 필터) → **전표 재배정**(다른 사업 선택 또는 신규 사업명 직접 입력 → override 저장) → ④ 다운로드. 프론트에는 분석 결과만 표출(원자료 파일 비노출).

### 문서
- **`알고리즘_명세.html`**(루트, 오프라인 self-contained) — 규칙 R1~R12 명세, WRatio 텍스트 매칭 실측 예시(용인 무정전 65점 임의귀속 + 인지세 전표번호 묶음, 지능형헬멧 98점 클러스터), 총액 보존 불변식, 사람 개입 지점.

### 검증 (23년 실데이터, DB 파이프라인)
- 손익 71,848,928 + 자본 55,749,751 + 미반영 386,074 = **127,984,753 ≒ ERP 전체(−1천원 반올림)**. 흡수 12s, 분석 손익 33s·자본 20s.

---

## 2026-08-03 · 23년 검증 반영 — 예산구분 분리·본사귀속·출력양식 (rev4)

**대상:** `budget_app` 2단계(실적 분석). 테스트 **49 passed**.
**검증 기준:** `표준화 검증 자료(23년)` — 업로드 원본 + 사용자 수기 분석본 `(23년) 손익/자본예산 실적 분석_260313.xlsx`.

### 오류 수정
1. **예산구분 오염 제거(치명).** 손익·자본이 섞인 계획 원본을 그대로 올리면 상대 예산 계획행이 전부 '미시행'으로 산출물에 섞였다(23년 손익본에 자본 229행·787억 혼입). 이제 `pipeline_actual`이 **이번 예산의 실적 대상 과목만** 남기고, 제외분은 검토리포트 '계획행 제외 내역'에 사유별(타예산/미선택/실적반영해제/미분류/처지사 미등록)로 보고.
2. **zrfm2 합계행·타연도 전표 제외.** zrfm2 마지막 줄 총계(23년 127,984,754천원)와 선택연도 외 전표를 `erp_loader.tag_excluded`가 표기 → 집계 제외, `zrfm2_V1` 반영구분에 `제외(합계행)`/`제외(타연도)`.
3. **본사 전표 강제귀속 폐지.** I열 `본　　사`인데 J열로도 처지사를 특정 못 하는 전표를 플랜트기술처로 몰아넣던 폴백 제거 → 미매핑으로 남기고 상세 보고. 23년 손익 314,252천원(경영지원처 총무부·정보시스템처)이 플랜트기술처 실적으로 과대계상되던 문제 해소, 분석본과 동일 기준이 됨(25년도 −188,787천원 동일 효과). `처지사_별칭.json`에 **`"I열|J열": "처지사"` 복합키** 지정 지원.
4. **요약·양식1·종합표 총액 불일치 해소.** 요약을 행별 반올림 합 기준으로 통일(이전 손익 16천원·자본 4천원 차이). ERP 원천 대비 반올림차이는 `반올림차이(천원)`로 공시.
5. **config JSON에 `NaN` 기록되던 버그.** `st.data_editor`가 만든 NaN이 `과목구성_*.json`에 비표준 토큰으로 저장되고, NaN이 truthy라 `실적반영` 판정이 뒤집힐 수 있었다. `config_store.to_bool`로 저장·로드 양쪽 정규화 + `allow_nan=False`.
6. **지사 정규화 범위 오류.** 포함 해제된 지사가 '미매핑'으로 오분류되던 문제 → 정규화는 **전체 지사명** 기준, 선택 필터는 매칭 단계에서만.
7. **그룹 내 임의 귀속 가시화.** 사업명 유사도 미달로 그룹 내 계획행에 배분된 전표를 `임의귀속`으로 집계·표기(양식1 비고, 검토리포트). 동점 시 연예산 큰 계획행 우선(입력순서 의존 제거).

### 산출물 변경
- **데이터시트 = 분석본 `[Sheet1]` 포맷.** 시트명 `Sheet1`, 1행 단위표기·2행 헤더·3행부터 데이터. A~J는 분석본과 동일(`연번·예산과목·속성·주관부서명·예산귀속 부서명(처.지사)·예산귀속 부서명(팀)·사업명·연예산(A)·최종 실적금액(B)·구 분`), K `사업유형`·L `분류키(=J&K)`는 수기 분류용 빈 열, M~P는 엔진 분석열(`집행구분·심의플래그·비고·매칭확신도`).
- **신규(계획에 없는 집행)를 사업 단위 1행으로 분해**(기존 과목×지사 묶음 → 사업별). 분석본의 '1억 이상 비정기사업' 분석과 동일 입도. 소액(≤2,000천원) 일괄확정은 유지. 신규행의 부서명(부)은 ERP J열 최빈값으로 채움.
- **파일명에 대상연도**: `zrfm2_V1(자본).xlsx` → **`zrfm2_2023_V1(자본).xlsx`**.
- **검토리포트 확대**: 점검 필요 사항(경고) · 예산과목별 계획 대비 실적(집행률) · 계획행 제외 내역 · 집계 제외 전표 · 미매핑 상세(I열/J열/과목/금액/건수).
- **대조 리포트** `output/대조_분석본vs자동분석_2023.xlsx`(과목별·과목×지사 차이).

### 23년 검증 결과
- **손익**: 자동 71,848,934 vs 분석본 71,889,218 → **−40,284천원(−0.06%)**.
- **자본**: 자동 55,749,753 vs 분석본 69,206,460 → −13,456,707천원. 전액 **ERP 소스 격차**: `외주비-열원정기점검`(A급정비) 11,416,005천원은 **23년 zrfm2에 계정 60602015 자체가 없음**(25년 추출본엔 존재 → 재추출 필요), 건설공사 3과목 4,518,228천원은 종전과 같이 zrfm2 범위 밖.
- 23년 config 정비: 지사그룹을 분석본 기준으로(대구·청주 → 소형CHP), 손익 심의대상 해제(분석본 종합표에 이상/미만 분리 없음).

---

## 2026-07-22 · 연동 어댑터 · 실행파일 · GitHub 배포 (rev3)

**대상:** `budget_app`. 테스트 **44 passed**.

- **연동규격_INTERFACE 어댑터 `src/budget/spec_io.py`** 코드화 — data/budget/matched/classified CSV 읽기·쓰기, 원↔천원 변환, 부서코드 앞3자리→처지사 매핑. `erp_loader`에 전기일·사업장(손익센터)·거래처(공급업체) 컬럼 추가. `matching`에 `_clean_na` 가드 보강. 테스트 `test_spec_io.py`(10). gap 명시: 투자유형=미분류(보류)·사업코드 공란·거래처명=코드.
- **실행파일**: `run.bat`(Windows)·`run.sh`(Mac·Linux)·`README_실행방법.md`. Python 확인→패키지 자동설치→streamlit 실행. `requirements.txt`에 **rapidfuzz 추가**(누락 보완).
- **GitHub 공개 배포**: `enhelee/ThreeON_Budget`(PUBLIC) `docs/superpowers/예산 실적 집계/`에 실행 코드 업로드(커밋 `3230c73`). **실데이터 전량 제외**(코드+빈템플릿+실행파일만) — 오프라인 분리망 규정 준수.
- 결정(사용자): 투자유형 담당 보류 · 입력은 우리것만 · 손익/자본 분리 유지.
- 문서: `연동규격_INTERFACE.md`·`연동_통합가이드.md`·`연동_필드매핑.md`(루트).

---

## 2026-07-22 · 실적 양식1 취합양식화 · 구성 선택 · 미반영 (rev2)

**대상:** `budget_app` 2단계(실적 분석). 테스트 **34 passed**.

### 무엇이 바뀌었나
1. **실적 양식1(월별) 포맷 = 취합양식(양식2/3)의 `계획 대비 실적` 그대로 채택.**
   - 13열: 연번·예산과목·속성·주관부서명·예산귀속 부서명(처.지사)·예산귀속 부서명(부)·사업명·연예산(A)·최종 실적금액(B) + 심의플래그·구분·비고·매칭확신도.
   - 종합표 SUMIFS 기준열을 파라미터화(`write_summary_formulas`의 `value/item/dept/flag_col`) → 계획본 G/C/L/J, 실적본 B/E/J/I. 재배치 후에도 총액 정합.
2. **실적 탭에 지사·예산과목 구성 편집기 추가** (계획 탭과 **동일 config 공유**).
   - **선택(포함)된 과목·지사만** 실적본 양식1·종합표에 반영. 포함 해제 과목/지사의 계획행은 실적본에서 제외.
3. **미반영 / zrfm2_V1 반영구분.**
   - 포함 해제·미분류·미매핑 처지사 전표 → `zrfm2_V1`의 **T열(20) 반영구분='미반영'**(연회색). R(매칭사업명)·S(매칭확신도) 유지.
   - 검토리포트에 미반영 금액·과목/지사별 상위 집계. matched CSV `구분`에 `미반영` 값 추가.
   - **정합식: 실적 종합표 총액 + 미반영 + 타예산(공란) = 이번 예산 ERP 전체(유실 0).**
4. **과목구성 `실적반영` 플래그(기본 true).** false면 계획본엔 포함·실적 산출물에서만 제외.
   - **건설공사 3과목**(외주비-열원공사비·재료비-열원자재비·외주비-열원기술용역비) = `실적반영:false` (zrfm2에 계정 없음).
5. 실적 양식1 6열 라벨 = `예산귀속 부서명(부)` (원데이터 수준에 맞춤).

### 변경 코드 (`src/budget/`)
`excel_actual_writer`(양식1 13열·반영구분·미반영 리포트) · `excel_plan_writer`(SUMIFS 기준열 파라미터화) ·
`matching`(선택 지사 필터·미반영 분류, `match_actuals`에 `selected_depts/budget_all/other_budget_all` 추가) ·
`pipeline_actual`(선택항목 필터·실적반영·미반영 집계) · `config_store`(건설공사 실적반영:false) · `app.py`(실적 탭 구성편집기).

### Step 1 종합표 금액 검증 결과 (vs 수기 취합본 _R1)
| 구분 | 우리(자동) | 참조(수기 _R1) | 차이 | 규명 |
|---|--:|--:|--:|---|
| 손익 | 98,554,801 | 98,345,148 | +209,651 (0.21%) | 경상정비 39,368,679 **정확 일치**. 건물/구축물 본사귀속 완전포착 +204,889(참조 0), 나머지 반올림 |
| 자본 | 49,709,178 | 66,172,008 | -16,462,832 | 건설공사 -17,299,921(zrfm2 계정없음, 범위밖) + 기계장치/구축물 +837,788(완전포착) |

- **경상정비**: 이전 "우리 vs 수기" 최대 쟁점 → 참조 _R1이 지사별 실수치로 채워 우리 값과 정확 일치(우리가 옳았음 확증).
- 차이는 모두 **"완전포착 vs 수기 재분류/누락" 또는 "데이터소스 범위"**로 규명. 엔진 정상.
- 전표추적 리포트: `output/검토_기계장치구축물_전표추적_2025자본.xlsx`, `output/검토_건물구축물_전표추적_2025손익.xlsx`.

### 산출물 (`budget_app/output/`)
`2025년 {손익|자본}예산_실적.xlsx`, `zrfm2_V1({손익|자본}).xlsx`, `matched_2025_{손익|자본}.csv`, 전표추적 리포트 2종.

### 설계 문서
`budget_app/docs/superpowers/specs/2026-07-22-실적탭-구성선택-미반영-design.md`

---

## 2026-07-22 · 1·2단계 최초 구축
계획본 생성(1단계) + 실적 분석(2단계) 구축. 상세 [HANDOFF.md](HANDOFF.md) · [연계규격_INTEGRATION.md](연계규격_INTEGRATION.md) · [PRD_V1.html](PRD_V1.html).
