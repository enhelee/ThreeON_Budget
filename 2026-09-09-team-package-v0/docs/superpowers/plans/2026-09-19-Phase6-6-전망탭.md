# Phase 6-6 구현 계획 — 5번 탭 실구현 (3·4단계 API · iframe 제거 · 엑셀 import 승계)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Streamlit iframe 을 걷어내고 표준화(3단계)·중장기 전망(4단계)을 budget_app 의 DB·API·SPA 안에서 돌린다.

**Architecture:** 계산부(`benchmark`·`forecast_calc`)와 상태 층(`forecast_store`, PR #24)은 이미 있다. 이번에 만드는 것은 세 층 — ① 입력 조립·실행 `pipeline_forecast.py`(마감 연도 `biz_line` → 실적, `plan_row` → 당해년도 예산), ② `api_forecast.py` 라우터(7개 경로), ③ `views/forecast.js` 재작성(기준연도 선택기 + 하위 탭 3개) — 과 v2 엑셀 파서 4종 승계 `forecast_import.py` 다. 결과는 저장하지 않고 요청마다 계산한다.

**Tech Stack:** Python 3.14 · FastAPI(APIRouter) · pandas · openpyxl · SQLite/PostgreSQL(dbcore) · Vite + Vanilla JS · pytest

**Spec:** [2026-09-19-Phase6-6-전망탭-design.md](../specs/2026-09-19-Phase6-6-전망탭-design.md) · 계약 [2026-09-19-전망포팅-계약.md](../specs/2026-09-19-전망포팅-계약.md)

## Global Constraints

- 작업 폴더 `2026-09-09-team-package-v0/app`. 브랜치 `phase6-forecast-tab` (PR #24 `phase6-forecast-store` 위에 쌓임 — #24 병합 뒤 rebase).
- 기준선 **budget_app 202 passed** · **전망 앱 183 passed**(원본 미변경). 어떤 태스크도 기존 테스트를 깨뜨린 채 끝내지 않는다.
- **단위 천원.** v2 파서의 ×1000 환산은 넣지 않는다.
- **3단계 모집단 = 마감된 연도만**, 구분 ∈ {계획집행, 신규, 신규(소액집행)} (사용자 결정 2026-09-19).
- **기준연도(base_year)는 5번 탭 자체 선택기**, 헤더 대상연도와 독립. 기본값 = 상태가 있는 최신 기준연도, 없으면 마감 최신연도+1.
- 지사 목록 = `dept_config(base_year)` 포함 지사 중 그룹 ≠ 본사.
- SQL 은 `?` 자리표시자. INSERT 는 컬럼 명시.
- 프론트 수정 후 `cd app/web && npm run build`. 브라우저 확인은 `?cb=값` + 번들 해시 확인.
- 로컬 서버는 `DATABASE_URL=" "`(공백 한 칸) · `APP_PASSWORD=" "` · `DATA_DIR=<scratch>`.
- 커밋 메시지 한국어, 본문에 «왜». 끝에 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- 엑셀 **내보내기**는 6-7, Caddy·Streamlit·supervisord 삭제는 6-8 — 이 계획에서 건드리지 않는다.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `src/budget/forecast_calc.py` (수정) | `base_year`·`years` 선택 인자. 기본값 2026 유지 |
| `src/budget/pipeline_forecast.py` (신규) | 마감 실적·계획본 어댑터·지사 목록·기본 기준연도·`run_benchmark`·`run_forecast` |
| `src/budget/forecast_import.py` (신규) | 엑셀 파서 4종(정비등급 이력·정기점검 일정·고온부품·본사배분). 파싱만, 저장 안 함 |
| `src/budget/api_forecast.py` (신규) | `make_router(conn_factory, clean_json)` → APIRouter `/api/forecast/*` |
| `server.py` (수정) | 라우터 include · `FORECAST_URL`·`forecast_url` 제거 |
| `web/src/state.js` (수정) | `state.fc` |
| `web/src/data.js` (수정) | `loadForecastMeta`·`loadForecastState`·`loadForecastBench`·`loadForecastTable` |
| `web/src/router.js` (수정) | forecast 화면의 로드 잡 · `mountForecast` 제거 |
| `web/src/views/forecast.js` (재작성) | 기준연도 선택기 + 하위 탭 3개 렌더 |
| `web/src/forecast_actions.js` (신규) | 5번 탭 클릭·change·파일 핸들러. `main.js` 가 위임 |
| `web/src/main.js` (수정) | 위임 두 줄 + 숨은 파일 입력 |
| `web/src/views/home.js` (수정) | 이음새 문구 |
| `tests/test_forecast_calc.py` (추가) · `tests/test_pipeline_forecast.py` · `tests/test_forecast_import.py` · `tests/test_forecast_api.py` (신규) | |

---

### Task 1: `forecast_calc` 기준연도 파라미터화

**Files:** Modify `src/budget/forecast_calc.py` · Test `tests/test_forecast_calc.py`

**Interfaces:**
- Produces: `forecast_years(base_year=BASE_YEAR) -> list[int]` · `year_multiplier(year, factors_df, base_year=BASE_YEAR)` · `compute_site_table(..., budget_lookup=None, base_year=BASE_YEAR, years=None)` · `compute_all_sites(..., budget_df=None, base_year=BASE_YEAR, years=None)`

- [ ] **Step 1: 실패하는 테스트** — `tests/test_forecast_calc.py` 끝에 추가

```python
# ---------------------------------------------------------------- 6-6: 기준연도 파라미터
def test_forecast_years_follow_base_year():
    assert lf.forecast_years() == list(range(2026, 2036))
    assert lf.forecast_years(2027) == list(range(2027, 2037))


def test_year_multiplier_uses_given_base_year():
    factors = pd.DataFrame([{"팩터명": "물가상승률", "연간비율": 0.1, "활성": True}])
    assert lf.year_multiplier(2027, factors, base_year=2027) == pytest.approx(1.0)
    assert lf.year_multiplier(2028, factors, base_year=2027) == pytest.approx(1.1)


def test_compute_site_table_columns_follow_base_year():
    grade_hist = pd.DataFrame({"사업장": ["화성지사"], "연도": [2027], "등급": ["간이"]})
    standard_df = pd.DataFrame([
        {"사업장": "화성지사", "구분": "손익", "예산과목": "수선유지비-열원경상정비", "등급": "간이", "표준금액": 100.0}])
    factors = pd.DataFrame([{"팩터명": "물가상승률", "연간비율": 0.1, "활성": True}])
    empty_hot = pd.DataFrame(columns=["사업장", "연도", "항목", "금액"])
    empty_temp_alloc = pd.DataFrame(columns=["사업장", "연도", "예산과목", "금액"])
    empty_surprise = pd.DataFrame(columns=["사업장", "연도", "예산과목", "금액", "사유"])
    empty_investment = pd.DataFrame(columns=["사업장", "지사유형", "금액"])
    table = lf.compute_site_table("화성지사", grade_hist, standard_df, empty_hot, {}, empty_temp_alloc,
                                   factors, empty_surprise, empty_investment, base_year=2027)
    assert [c for c in table.columns if c != "예산과목"] == list(range(2027, 2037))
    row = table[table["예산과목"] == "수선유지비-열원경상정비"].iloc[0]
    assert row[2027] == pytest.approx(100.0)          # 기준연도는 배수 1
    assert row[2028] == pytest.approx(110.0)
```

- [ ] **Step 2: 실패 확인** — `py -m pytest tests/test_forecast_calc.py -q` → `AttributeError: forecast_years` / `TypeError: base_year`
- [ ] **Step 3: 구현** — `BASE_YEAR`·`FORECAST_YEARS` 상수는 남기고(v2 대조용) 함수를 추가·수정

```python
def forecast_years(base_year: int = BASE_YEAR) -> list:
    """기준연도부터 10개년. v2 의 FORECAST_YEARS(2026~2035)는 base_year=2026 인 경우다."""
    return list(range(int(base_year), int(base_year) + 10))

def year_multiplier(year, factors_df, base_year=BASE_YEAR):
    n = year - int(base_year)
    ...  # 나머지 동일

def compute_site_table(site, grade_hist, standard_df, hot_parts_df, hq_amount_by_site_acct,
                       hq_temp_alloc, factors_df, surprise_df, investment_df,
                       budget_lookup=None, base_year=BASE_YEAR, years=None):
    base_year = int(base_year)
    years = list(years) if years else forecast_years(base_year)
    # 본문에서 FORECAST_YEARS → years, BASE_YEAR → base_year, year_multiplier(year, factors_df) → year_multiplier(year, factors_df, base_year)

def compute_all_sites(..., budget_df=None, base_year=BASE_YEAR, years=None):
    ... compute_site_table(..., budget_lookup, base_year=base_year, years=years)
```

- [ ] **Step 4: 통과 확인** — `py -m pytest tests/test_forecast_calc.py tests/test_forecast_store.py -q` (기존 18 + 새 3 + 23)
- [ ] **Step 5: 커밋** — `refactor: forecast_calc 기준연도를 인자로 — 기본값 2026 은 그대로 (6-6 Task 1)`

---

### Task 2: `pipeline_forecast.py` — 입력 조립과 실행

**Files:** Create `src/budget/pipeline_forecast.py` · Test `tests/test_pipeline_forecast.py`

**Interfaces:**
- Consumes: `dbm.locked_years(conn) -> list[str]` · `dbm.latest_run(conn, year, budget) -> dict|None` · `dbm.load_plan_df(conn, year) -> DataFrame|None` · `config_store.load_dept_config` · `forecast_store.*` · `forecast_calc.compute_all_sites(..., base_year, years)`
- Produces:
  - `KEEP = ("계획집행", "신규", "신규(소액집행)")` · `ACTUAL_COLS` · `DEPT_COL = "예산귀속 \n부서코드"` · `AMOUNT_COL = "연예산 합계"`
  - `locked_actuals(conn) -> (DataFrame[사업장,연도,예산과목,금액], years_used: list[str])`
  - `budget_plan_for(conn, base_year) -> DataFrame[예산과목, DEPT_COL, AMOUNT_COL]`
  - `forecast_sites(conn, base_year) -> list[str]` · `site_groups(conn, base_year) -> dict`
  - `default_base_year(conn) -> str`
  - `run_benchmark(conn, base_year) -> dict{base_year, years_used, population{rows,total}, has_ltsa, rows}`
  - `run_forecast(conn, base_year) -> dict{base_year, years, sites, tables{site: rows}, total_rows, notes}`

- [ ] **Step 1: 실패하는 테스트** — `tests/test_pipeline_forecast.py`

```python
# -*- coding: utf-8 -*-
"""3·4단계 입력 조립 — 마감 연도 실적 · 계획본 어댑터 · 지사 목록 · 실행."""
import json

import pandas as pd
import pytest

from budget import config_store
from budget import db as dbm
from budget import forecast_store as fs
from budget import pipeline_forecast as pf


@pytest.fixture()
def conn(tmp_path):
    c = dbm.connect(str(tmp_path / "t.db"))
    yield c
    c.close()


def _seed_run(conn, year, budget, lines):
    """lines: [(처지사, 예산과목, 실적금액, 구분)]"""
    cur = conn.execute("INSERT INTO run(year,budget,created_at,summary_json) VALUES(?,?,?,?)",
                       (year, budget, "2026-01-01 00:00:00", json.dumps({})))
    run_id = cur.lastrowid
    for dept, item, amt, gubun in lines:
        conn.execute("INSERT INTO biz_line(run_id,budget,예산과목,처지사,사업명,연예산,실적금액,구분)"
                     " VALUES(?,?,?,?,?,?,?,?)", (run_id, budget, item, dept, "x", 0.0, amt, gubun))
    conn.commit()
    return run_id


def _seed_plan(conn, year, rows):
    """rows: [(처지사, 예산과목, 연예산)]"""
    cur = conn.execute("INSERT INTO dataset(kind,year,label,uploaded_at) VALUES('plan',?,?,?)",
                       (year, "plan.xlsx", "2026-01-01 00:00:00"))
    ds = cur.lastrowid
    for i, (dept, item, amt) in enumerate(rows):
        conn.execute("INSERT INTO plan_row(dataset_id,주관부서명,부서코드,처지사,부서부,속성,예산코드,예산과목,"
                     "사업명,산출내역,연예산,src_row) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)",
                     (ds, "부", "C", dept, "", "일반", "K", item, f"사업{i}", "", amt, i + 4))
    conn.commit()


def test_locked_actuals_uses_only_locked_years_and_kept_gubun(conn):
    _seed_run(conn, "2023", "손익", [("화성지사", "수선유지비-열원경상정비", 100.0, "계획집행"),
                                     ("화성지사", "수선유지비-열원경상정비", 5.0, "미시행"),
                                     ("화성지사", "기계장치", 7.0, "신규(소액집행)")])
    _seed_run(conn, "2025", "손익", [("화성지사", "수선유지비-열원경상정비", 200.0, "신규")])
    dbm.lock_year(conn, "2023")            # 2025 는 열려 있다

    df, years = pf.locked_actuals(conn)
    assert years == ["2023"]
    assert list(df.columns) == ["사업장", "연도", "예산과목", "금액"]
    assert df["금액"].sum() == pytest.approx(107.0)
    assert set(df["연도"]) == {2023}
    assert "미시행" not in set(df.get("구분", []))


def test_locked_actuals_empty_when_no_locked_run(conn):
    df, years = pf.locked_actuals(conn)
    assert df.empty and years == []
    assert list(df.columns) == ["사업장", "연도", "예산과목", "금액"]


def test_budget_plan_for_adapts_plan_rows_to_v2_columns(conn):
    _seed_plan(conn, "2026", [("화성지사", "기계장치", 1000.0), ("화성지사", "기계장치", 500.0),
                              ("동탄지사", "외주비-열원공사비", 300.0)])
    df = pf.budget_plan_for(conn, "2026")
    assert list(df.columns) == ["예산과목", pf.DEPT_COL, pf.AMOUNT_COL]
    assert df[pf.AMOUNT_COL].sum() == pytest.approx(1800.0)
    assert set(df[pf.DEPT_COL]) == {"화성지사", "동탄지사"}


def test_budget_plan_for_missing_year_is_empty_frame(conn):
    df = pf.budget_plan_for(conn, "2031")
    assert df.empty and list(df.columns) == ["예산과목", pf.DEPT_COL, pf.AMOUNT_COL]


def test_forecast_sites_exclude_hq_and_unincluded(conn):
    cfg = config_store.load_dept_config(conn, "2026")
    for x in cfg:
        if x["이름"] == "평택지사":
            x["포함"] = False
    config_store.save_dept_config(conn, "2026", cfg)
    sites = pf.forecast_sites(conn, "2026")
    assert "화성지사" in sites and "양산지사" in sites
    assert "플랜트기술처" not in sites            # 본사 그룹 제외
    assert "평택지사" not in sites                # 포함 해제
    assert pf.site_groups(conn, "2026")["양산지사"] == "중대형CHP"


def test_default_base_year_prefers_existing_state_then_locked_plus_one(conn):
    assert pf.default_base_year(conn) == "2026"          # 아무것도 없음 → v2 기본
    _seed_run(conn, "2024", "손익", [("화성지사", "기계장치", 1.0, "계획집행")])
    dbm.lock_year(conn, "2024")
    assert pf.default_base_year(conn) == "2025"          # 마감 최신 + 1
    fs.save_hq_ratio(conn, "2028", pd.DataFrame([{"사업장": "화성지사", "계약체결금액": 1.0}]))
    assert pf.default_base_year(conn) == "2028"          # 상태가 있으면 그 최신


def test_run_benchmark_and_forecast_end_to_end(conn):
    _seed_run(conn, "2023", "손익", [("화성지사", "수선유지비-열원경상정비", 1000.0, "계획집행")])
    _seed_run(conn, "2025", "손익", [("화성지사", "수선유지비-열원경상정비", 1100.0, "계획집행")])
    dbm.lock_year(conn, "2023"); dbm.lock_year(conn, "2025")
    _seed_plan(conn, "2026", [("화성지사", "수선유지비-열원경상정비", 1200.0),
                              ("화성지사", "외주비-열원공사비", 50.0)])
    fs.save_factors(conn, "2026", pd.DataFrame([{"팩터명": "물가상승률", "연간비율": 0.1, "활성": True}]))

    bench = pf.run_benchmark(conn, "2026")
    assert bench["years_used"] == ["2023", "2025"]
    assert bench["population"] == {"rows": 2, "total": 2100.0}
    row = next(r for r in bench["rows"] if r["사업장"] == "화성지사")
    assert row["예산과목"] == "수선유지비-열원경상정비" and row["산출방식"] == "최근실적"
    assert row["표준금액"] == pytest.approx(1100.0)

    out = pf.run_forecast(conn, "2026")
    assert out["years"] == list(range(2026, 2036))
    assert "화성지사" in out["sites"] and "플랜트기술처" not in out["sites"]
    hs = {r["예산과목"]: r for r in out["tables"]["화성지사"]}
    line = hs["수선유지비-열원경상정비"]
    assert line["2026"] == pytest.approx(1200.0)          # 당해년도 = 계획본
    assert line["2027"] == pytest.approx(1100.0 * 1.1)    # 다음년도 = 표준금액 × 팩터
    assert hs["투자비"]["2026"] == pytest.approx(50.0)     # 외주비-열원공사비 = 투자비
    assert out["notes"]["budget_plan_missing"] is False
    total = {r["예산과목"]: r for r in out["total_rows"]}
    assert total["수선유지비-열원경상정비"]["2026"] == pytest.approx(1200.0)
```

- [ ] **Step 2: 실패 확인** — `py -m pytest tests/test_pipeline_forecast.py -q` → ModuleNotFoundError
- [ ] **Step 3: 구현** — `src/budget/pipeline_forecast.py`

```python
# -*- coding: utf-8 -*-
"""3·4단계 입력 조립과 실행 — UI 비의존. (모듈 docstring 은 설계 §2·§3.1 의 요지)"""
import pandas as pd

from . import benchmark, config_store, forecast_calc, forecast_store as fs
from . import db as dbm

KEEP = ("계획집행", "신규", "신규(소액집행)")
ACTUAL_COLS = ["사업장", "연도", "예산과목", "금액"]
DEPT_COL = "예산귀속 \n부서코드"      # v2 예산계획의 열 이름 — 계산부(forecast_calc)가 이 이름을 본다
AMOUNT_COL = "연예산 합계"
BUDGET_COLS = ["예산과목", DEPT_COL, AMOUNT_COL]


def locked_actuals(conn):
    years_used, rows = [], []
    for year in sorted(dbm.locked_years(conn)):
        used = False
        for budget in ("손익", "자본"):
            run = dbm.latest_run(conn, year, budget)
            if not run:
                continue
            used = True
            for dept, item, amt, gubun in conn.execute(
                    "SELECT 처지사,예산과목,실적금액,구분 FROM biz_line WHERE run_id=?", (run["id"],)):
                if gubun in KEEP and amt:
                    rows.append({"사업장": dept, "연도": int(year), "예산과목": item, "금액": float(amt)})
        if used:
            years_used.append(str(year))
    return pd.DataFrame(rows, columns=ACTUAL_COLS), years_used


def budget_plan_for(conn, base_year):
    df = dbm.load_plan_df(conn, base_year)
    if df is None or df.empty:
        return pd.DataFrame(columns=BUDGET_COLS)
    return pd.DataFrame({"예산과목": df["예산과목"].astype(str), DEPT_COL: df["처지사"].astype(str),
                         AMOUNT_COL: pd.to_numeric(df["연예산"], errors="coerce").fillna(0.0)},
                        columns=BUDGET_COLS)


def forecast_sites(conn, base_year):
    return [x["이름"] for x in config_store.load_dept_config(conn, base_year)
            if x["포함"] and x["그룹"] != "본사"]


def site_groups(conn, base_year):
    return {x["이름"]: x["그룹"] for x in config_store.load_dept_config(conn, base_year)}


def default_base_year(conn):
    existing = fs.list_base_years(conn)
    if existing:
        return existing[-1]
    locked = dbm.locked_years(conn)
    return str(int(max(locked)) + 1) if locked else str(forecast_calc.BASE_YEAR)


def _records(df):
    import json
    return json.loads(df.to_json(orient="records", force_ascii=False)) if not df.empty else []


def _standard(conn, base_year):
    actuals, years_used = locked_actuals(conn)
    grade = fs.load_grade_history(conn, base_year)
    std = benchmark.compute_standard_amounts(actuals, grade, fs.load_method_map(conn, base_year),
                                             fs.load_overrides(conn, base_year),
                                             item_alias=config_store.load_item_alias(conn))
    return std, actuals, years_used, grade


def run_benchmark(conn, base_year):
    fs.bind_site_resolvers(conn, base_year)
    std, actuals, years_used, _ = _standard(conn, base_year)
    std = std.sort_values(["사업장", "구분", "예산과목", "등급"]) if not std.empty else std
    return {"base_year": str(base_year), "years_used": years_used,
            "population": {"rows": int(len(actuals)), "total": float(actuals["금액"].sum()) if len(actuals) else 0.0},
            "has_ltsa": benchmark.has_ltsa_detail(actuals), "rows": _records(std)}


def run_forecast(conn, base_year):
    fs.bind_site_resolvers(conn, base_year)
    by = int(base_year)
    years = forecast_calc.forecast_years(by)
    std, actuals, years_used, grade = _standard(conn, base_year)
    sites = forecast_sites(conn, base_year)
    budget_df = budget_plan_for(conn, base_year)
    tables = forecast_calc.compute_all_sites(
        sites, grade, std, fs.load_hot_parts(conn, base_year), fs.load_hq_master(conn, base_year),
        fs.load_hq_temp_projects(conn, base_year), fs.load_hq_ratio(conn, base_year),
        fs.load_factors(conn, base_year), fs.load_surprise_projects(conn, base_year),
        forecast_calc.investment_by_site(budget_df), budget_df, base_year=by, years=years)
    def rows_of(df):
        return [{"예산과목": r["예산과목"], **{str(y): float(r[y]) for y in years}} for _, r in df.iterrows()]
    out_tables = {s: rows_of(t) for s, t in tables.items()}
    total = {}
    for rows in out_tables.values():
        for r in rows:
            acc = total.setdefault(r["예산과목"], {str(y): 0.0 for y in years})
            for y in years:
                acc[str(y)] += r[str(y)]
    total_rows = [{"예산과목": k, **v} for k, v in total.items()]
    return {"base_year": str(base_year), "years": years, "years_used": years_used, "sites": sites,
            "tables": out_tables, "total_rows": total_rows,
            "notes": {"budget_plan_missing": bool(budget_df.empty), "grade_empty": bool(grade.empty),
                      "actuals_empty": bool(actuals.empty)}}
```

- [ ] **Step 4: 통과 확인** — `py -m pytest tests/test_pipeline_forecast.py -q`
- [ ] **Step 5: 커밋** — `feat: 3·4단계 입력 조립 — 마감 연도 실적·계획본 어댑터·실행 (6-6 Task 2)`

---

### Task 3: `forecast_import.py` — 엑셀 파서 4종 승계

**Files:** Create `src/budget/forecast_import.py` · Test `tests/test_forecast_import.py`

**Interfaces:**
- Produces: `site_sheet_map(site_names) -> dict[short, name]` · `grades_from_workbook(data: bytes, site_names) -> DataFrame[사업장,연도,등급]` · `schedule_from_workbook(data, site_names) -> 같은 모양` · `hot_parts_from_workbook(data, site_names) -> DataFrame[사업장,연도,항목,금액]`(천원) · `hq_master_from_workbook(data, site_names) -> (wide DataFrame, ratio DataFrame)`

- [ ] **Step 1: 실패하는 테스트** — v2 `tests/test_standardization.py::_build_sample_workbook`·`test_longterm_forecast.py::_build_schedule_workbook`·고온부품·`_build_hq_master_workbook` 를 옮기고 `site_names=["화성지사","동탄지사","광주전남지사","수원사업소"]` 를 넘긴다. 고온부품 단언은 `== 1000`(천원 그대로). 시트명 매핑 테스트 하나 추가:

```python
def test_site_sheet_map_strips_suffix():
    m = fi.site_sheet_map(["화성지사", "수원사업소", "광주전남지사"])
    assert m == {"화성": "화성지사", "수원": "수원사업소", "광주전남": "광주전남지사"}
```

- [ ] **Step 2: 실패 확인** → ModuleNotFoundError
- [ ] **Step 3: 구현** — v2 함수 4개를 옮기되 `std._site_sheet_name_map()` 호출을 `site_sheet_map(site_names)` 로, `GT_GRADE_VALUES`·`ST_GRADE_VALUES`·`VALID_GRADE_TOKENS`·`_classify_grade_row` 는 `forecast_calc` 에서 가져온다. `hot_parts_from_workbook` 의 `float(amount) * 1000` 을 `float(amount)` 로.
- [ ] **Step 4: 통과 확인** — `py -m pytest tests/test_forecast_import.py -q`
- [ ] **Step 5: 커밋** — `feat: 전망 엑셀 파서 4종 승계 — 시트명은 dept_config 지사명으로, 단위 천원 (6-6 Task 3)`

---

### Task 4: `api_forecast.py` 라우터 + `server.py` 연결 + `FORECAST_URL` 제거

**Files:** Create `src/budget/api_forecast.py` · Modify `server.py`(`FORECAST_URL` 정의·`/healthz` 의 `forecast_url` 삭제, 정적 마운트 앞에 `app.include_router(api_forecast.make_router(_conn, _clean_json))`) · Test `tests/test_forecast_api.py`

**Interfaces:**
- Produces (모두 `/api/forecast` 아래):
  - `GET  /base-years` → `{base_years, default, locked_years}`
  - `POST /copy-base-year` `{from_year, to_year, overwrite=false}` → 400 같은 연도 · 409 손댄 대상 · `{to_year, base_years}`
  - `GET  /state?base_year=` → `{base_year, sites, site_groups, accounts{손익,자본}, grades, methods, overrides, factors, hot_parts, hq_master{columns,rows}, hq_ratio, hq_temp, surprise}`
  - `PUT  /state/{table}` `{base_year, rows}` (table ∈ grades·methods·overrides·factors·hot-parts·hq-ratio·hq-temp·surprise) / `hq-master` 는 `{base_year, columns, rows, ratio_rows?}` → `{table, rows}` 저장본
  - `POST /import?kind=&base_year=` (multipart file) → `{kind, rows}` / hq-master `{kind, columns, rows, ratio_rows}` · 400 모르는 kind·파싱 실패
  - `GET  /benchmark?base_year=` → `pipeline_forecast.run_benchmark`
  - `GET  /table?base_year=` → `pipeline_forecast.run_forecast`

- [ ] **Step 1: 실패하는 테스트** — `tests/test_forecast_api.py` (픽스처는 `test_config_api.py` 의 `client` 와 동일)

```python
def test_healthz_has_no_forecast_url(client):
    assert "forecast_url" not in client.get("/healthz").json()

def test_base_years_default_is_2026_when_nothing(client):
    r = client.get("/api/forecast/base-years").json()
    assert r == {"base_years": [], "default": "2026", "locked_years": []}

def test_state_put_get_round_trip_for_every_table(client):
    for table, rows in {
        "grades": [{"사업장": "화성지사", "연도": 2027, "등급": "TI"}],
        "methods": [{"사업장": "화성지사", "예산과목": "기계장치", "방식": "최근실적"}],
        "overrides": [{"사업장": "화성지사", "예산과목": "기계장치", "등급": "표준", "표준금액": 5.0}],
        "factors": [{"팩터명": "물가상승률", "연간비율": 0.02, "활성": True}, {"팩터명": "노후화", "연간비율": 0.01, "활성": False}],
        "hot-parts": [{"사업장": "화성지사", "연도": 2027, "항목": "고온부품재생", "금액": 10.0}],
        "hq-ratio": [{"사업장": "화성지사", "계약체결금액": 6000.0}],
        "hq-temp": [{"사업명": "a", "예산과목": "기계장치", "연도": 2027, "금액": 1.0}],
        "surprise": [{"사업장": "화성지사", "연도": 2028, "예산과목": "기계장치", "금액": 2.0, "사유": "x"}],
    }.items():
        r = client.put(f"/api/forecast/state/{table}", json={"base_year": "2026", "rows": rows})
        assert r.status_code == 200, (table, r.text)
    r = client.put("/api/forecast/state/hq-master", json={
        "base_year": "2026", "columns": ["대분류", "중분류", "세부내역", "26년예산", "화성지사"],
        "rows": [{"대분류": "플랜트", "중분류": "경상정비", "세부내역": "", "26년예산": 600, "화성지사": 600}],
        "ratio_rows": [{"사업장": "화성지사", "계약체결금액": 1.0}]})
    assert r.status_code == 200
    st = client.get("/api/forecast/state?base_year=2026").json()
    assert st["grades"][0]["등급"] == "TI"
    assert st["factors"][1]["활성"] is False
    assert st["hq_master"]["columns"][-1] == "화성지사" and st["hq_master"]["rows"][0]["화성지사"] == 600.0
    assert st["hq_ratio"] == [{"사업장": "화성지사", "계약체결금액": 1.0}]      # hq-master PUT 이 비율도 갈아 끼웠다
    assert "화성지사" in st["sites"] and st["site_groups"]["화성지사"] == "중대형CHP"
    assert "기계장치" in st["accounts"]["자본"]
    assert client.get("/api/forecast/base-years").json()["base_years"] == ["2026"]

def test_unknown_state_table_is_404(client):
    assert client.put("/api/forecast/state/nope", json={"base_year": "2026", "rows": []}).status_code == 404

def test_copy_base_year_guards_touched_target(client):
    client.put("/api/forecast/state/factors", json={"base_year": "2026", "rows": [{"팩터명": "물가상승률", "연간비율": 0.03, "활성": True}]})
    assert client.post("/api/forecast/copy-base-year", json={"from_year": "2026", "to_year": "2026"}).status_code == 400
    assert client.post("/api/forecast/copy-base-year", json={"from_year": "2026", "to_year": "2027"}).status_code == 200
    assert client.get("/api/forecast/state?base_year=2027").json()["factors"][0]["연간비율"] == 0.03
    client.put("/api/forecast/state/surprise", json={"base_year": "2027", "rows": [{"사업장": "화성지사", "연도": 2028, "예산과목": "기계장치", "금액": 1.0, "사유": ""}]})
    assert client.post("/api/forecast/copy-base-year", json={"from_year": "2026", "to_year": "2027"}).status_code == 409
    assert client.post("/api/forecast/copy-base-year", json={"from_year": "2026", "to_year": "2027", "overwrite": True}).status_code == 200

def test_import_preview_does_not_save(client, tmp_path):
    # v2 고온부품 워크북 빌더를 재사용해 파일을 만들고 업로드
    ...
    r = client.post("/api/forecast/import?kind=hot-parts&base_year=2026", files={"file": ("h.xlsx", data, "application/octet-stream")})
    assert r.status_code == 200 and r.json()["rows"][0]["금액"] == 1000.0
    assert client.get("/api/forecast/state?base_year=2026").json()["hot_parts"] == []
    assert client.post("/api/forecast/import?kind=nope&base_year=2026", files={"file": ("h.xlsx", data, "application/octet-stream")}).status_code == 400

def test_benchmark_and_table_read_locked_runs(client):
    # 마감 run 두 해 + 2026 계획본을 DB 에 직접 심는다(test_pipeline_forecast 의 시더와 동일 SQL)
    ...
    b = client.get("/api/forecast/benchmark?base_year=2026").json()
    assert b["years_used"] == ["2023", "2025"] and len(b["rows"]) == 1
    t = client.get("/api/forecast/table?base_year=2026").json()
    assert t["years"][0] == 2026 and "화성지사" in t["tables"]
```

- [ ] **Step 2: 실패 확인** → 404
- [ ] **Step 3: 구현** — `api_forecast.make_router(conn_factory, clean_json)`; 표 이름 → `forecast_store` 저장 함수 매핑 `SAVERS`; `_records` 는 `df.to_json(orient="records")` 경유(numpy → JSON 안전); import 는 `await file.read()` 후 `forecast_import.*(data, forecast_sites(conn, base_year))`; server 의 `/healthz` 에서 `forecast_url` 제거, `FORECAST_URL` 상수와 주석 삭제, 정적 마운트 직전 `app.include_router(...)`.
- [ ] **Step 4: 통과 확인** — `py -m pytest tests/test_forecast_api.py tests/test_config_api.py -q`
- [ ] **Step 5: 커밋** — `feat: 3·4단계 API — /api/forecast/* 7개 · FORECAST_URL 제거 (6-6 Task 4)`

---

### Task 5: 프론트 — 5번 탭 재작성, iframe 제거

**Files:** Modify `web/src/state.js` · `web/src/data.js` · `web/src/router.js` · `web/src/main.js` · `web/src/views/home.js` · Rewrite `web/src/views/forecast.js` · Create `web/src/forecast_actions.js`

**Interfaces:**
- `state.fc = {baseYear:"", baseYears:[], lockedYears:[], tab:"manage", data:null, bench:null, table:null, site:"", dirty:new Set(), preview:null, benchGroup:"all", benchEdits:{methods:{}, overrides:{}}}`
- `data.js`: `loadForecastMeta()`(base-years → `fc.baseYear` 기본값) · `loadForecastState()` · `loadForecastBench()` · `loadForecastTable()`
- `router.navigate("forecast")` 잡: 메타 → 상태, 탭이 `bench` 면 벤치마크, `table` 이면 표.
- `forecast_actions.js`: `export async function fcClick(e) -> bool` · `export async function fcChange(e) -> bool` · `export async function fcFile(e) -> bool`. `main.js` 의 click/change 리스너 첫 줄에서 `if (await fcClick(e)) return`.
- DOM 계약(`forecast.js` 가 그리고 `forecast_actions.js` 가 읽는다):
  - 기준연도 `select[data-fcbase]` · 버튼 `data-action="fc-add-base"` · 하위 탭 `[data-fctab=manage|bench|table]`
  - 표 편집 `[data-fc=<table>][data-idx][data-field]` (grades 는 `[data-fc=grade-cell][data-site][data-year]`) · 행 추가 `[data-fcadd=<table>]` · 삭제 `[data-fcdel=<table>][data-idx]` · 저장 `data-action="fc-save"`
  - 가져오기 `[data-fcimport=grades|schedule|hot-parts|hq-master]` → 숨은 `<input id="fcImportFile" type="file">` · 미리보기 적용 `data-action="fc-apply-merge"|"fc-apply-replace"|"fc-cancel-preview"`
  - 표준화 `select[data-fcbench=method][data-key]` · `input[data-fcbench=amount][data-key]` · 필터 `select[data-fcgroup]` · 저장 `data-action="fc-save-bench"`
  - 전망 `select[data-fcsite]`(값 `__total__` = 전사 합계)
- 화면 단위 표시는 천원(`fmt`). 하단에 `<a href="/forecast/" target="_blank">(구) 전망 앱 새 탭에서 열기</a>` 하나만(6-8 에서 제거).

- [ ] **Step 1: state·data·router** — `state.fc` 추가, 로더 4개, `navigate` 의 forecast 잡 교체, `mountForecast` import·호출 제거, `viewMeta.forecast` 부제 갱신.
- [ ] **Step 2: `forecast.js` 재작성** — `renderForecast()` = 헤더(기준연도·마감연도 안내·탭) + `manageBody()`(6 섹션) | `benchBody()` | `tableBody()` + 미리보기 패널 + 구 앱 링크.
- [ ] **Step 3: `forecast_actions.js`** — 위 DOM 계약대로. 저장은 dirty 표만 `PUT /api/forecast/state/<table>`; 벤치 저장은 `benchEdits` 를 기존 methods/overrides 에 병합해 두 표 PUT 후 `loadForecastBench()`; 가져오기 적용은 grades/schedule → grades 병합(없는 (사업장,연도)만 추가 | 전체 교체) 후 즉시 PUT, hot-parts → (사업장,연도,항목) 병합 | 교체, hq-master → 교체(비율 포함).
- [ ] **Step 4: `main.js` 위임 + `index.html` 숨은 입력 없음(뷰 안에 둔다)** · `home.js` bridgeBox 문구를 «마감된 연도의 분석 결과가 5번 탭의 입력. 팀연계 JSON 은 참고용»으로.
- [ ] **Step 5: 빌드** — `cd app/web && npm run build` 성공, `app/static/index.html` 번들 해시 기록.
- [ ] **Step 6: 커밋** — `feat: 5번 탭 실구현 — 기준연도·기준정보·표준화·전망 하위 탭, iframe 제거 (6-6 Task 5)`

---

### Task 6: 실측·회귀·문서·PR

- [ ] **Step 1: 전체 회귀** — `cd app && py -m pytest -q`(202 + 신규) · `cd forecast && py -m pytest tests -q`(183)
- [ ] **Step 2: 브라우저 실측** — 실DB 사본을 scratch 에 복사, `DATABASE_URL=" " APP_PASSWORD=" " DATA_DIR=<scratch> py -m uvicorn server:app --port 8099 --app-dir .` → `/healthz` 가 `db: sqlite` · `forecast_url` 없음 → `#/forecast?cb=1`: 기준연도 2026 기본 · 팩터 편집·저장 → 새로고침 후 유지 · 표준화 표(마감 2023·2025 기준, 203행 근처) · 전망 표(22개 지사) · 캡처.
- [ ] **Step 3: 문서** — 계획서(Phase6 총괄) 6-6 행 ✅ · 계약 §6 갱신(BASE_YEAR 파라미터화 완료·엑셀 import 승계 완료) · 로컬 인수인계·메모리.
- [ ] **Step 4: 커밋·푸시·PR** — base `main`(#24 병합 전이면 #24 커밋이 포함됨을 본문에 명시).

## Self-Review

- 스펙 §2 입력 → Task 2 · §3.1 → Task 2 · §3.2 → Task 1 · §3.3 → Task 3 · §3.4 → Task 4 · §4 → Task 5 · §5 → 각 태스크 테스트 + Task 6 · §7 → Task 6.
- 이름 일치: `DEPT_COL`/`AMOUNT_COL`(Task 2) ↔ `forecast_calc.investment_by_site` 가 읽는 `"예산귀속 \n부서코드"`·`"연예산 합계"` — 동일 리터럴. `forecast_years`(Task 1) ↔ Task 2 `run_forecast`. 표 이름 8+1(Task 4) ↔ Task 5 `data-fc` 값.
