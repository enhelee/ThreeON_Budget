# Phase 7 구현 계획 — 잔재 정리 · 팀 문서 최신화 · 최종 공유 자료

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 6 로 앱이 하나가 된 상태를 «동료가 30분 안에 파악하고 바로 쓰는» 수준으로 마감한다 — 죽은 코드를 지우고, 팀 문서를 사실에 맞추고, 공유 자료 HTML 한 장을 만든다.

**사용자 결정 (2026-09-20 밤):**

| 결정 | 내용 |
|---|---|
| 호스팅 | **Render 무료 유지**(Starter·Northflank·Vercel 검토 후). 첫 접속 30~60초 지연은 공유 자료에 안내 |
| 공유 자료 | **HTML 한 장** — 소개 · 사용법(5탭) · 검증 현황 · 알려진 한계 · 남은 일. 저장소 `docs/` 에 두고 배포본 화면 캡처 포함 |
| 저장소 실데이터 | **이번엔 그대로**(보류 유지). 공유 자료에는 저장소 링크 대신 **배포 주소**만 |
| `/api/export-team` | **삭제** — 소비자(v2)가 사라진 팀 연계 내보내기 4종·화면 패널·`spec_io`·테스트. `matched` CSV 는 `/api/export?kind=matched` 로 계속 |
| 알고리즘 고도화(계층적 후퇴) | **이번엔 «알려진 한계»로 적기만** — 대외 수치가 바뀌는 일이라 동료 합의 후 별도 Phase |

**Tech Stack:** FastAPI · Vite SPA · pytest · 배포본 캡처(로컬 uvicorn + 실DB 사본)

## Global Constraints

- 작업 폴더 `2026-09-09-team-package-v0`. **기준선 283 passed**(`d9644e9`).
- 프론트 수정 후 `cd app/web && npm run build`. 커밋 한국어·«왜»·`Co-Authored-By`.
- 로컬 서버는 반드시 `DATABASE_URL=" "`(공백 한 칸) — 비우면 운영 PG 에 붙는다.
- 캡처는 **로컬 사본**(실DB 복사본 + `/api/lock` 으로 마감)에서 — 배포본 로그인은 팀 비밀번호가 필요해 Claude 가 하지 않는다.

---

## Task 1: v2 잔재 삭제 — `/api/export-team` · `/api/export-status` · `spec_io` · 화면 패널

**왜 `export-status` 까지:** 그 엔드포인트는 «팀 연계 파일을 마지막으로 넘긴 시각»만 읽는다. 넘길 곳이 없으니 홈의 «이음새» 박스도 의미가 없다.

- [ ] **Step 1: 테스트 먼저** — `test_export_status.py` 에서 export-status 4개 삭제(healthz·index no-cache·`/app/` 301 은 남김 → 파일명은 그대로, docstring 갱신) · `test_spec_io.py`·`test_team_bundle.py` 삭제 · `test_docs_consolidation.py` 의 통합본 필수어에서 `/api/export-team`·`schemaVersion`·`sourceTotals`·`미배정` 제거, 대신 «`/api/export?kind=`·`import_v2_csv_dir`» 유지 · **부재 고정**: `server.py` 에 `export-team`/`export-status` 라우트 없음, `spec_io.py` 부재, 화면 소스에 `teamExport`/`exportStatus` 없음
- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 서버** — `server.py` 두 라우트 + `_mtime_str`(다른 사용처 없으면) 삭제 · `pipeline_db.py` 의 `TEAM_BUNDLE_FILES`·`team_bundle_paths`·`export_team_bundle` 삭제 · `src/budget/spec_io.py` 삭제 · `scripts/v2_model_sync.py` 삭제(v2 폴더와 모델을 주고받던 스크립트 — 대상 소멸)
- [ ] **Step 4: 화면** — `stats.js` «팀 연계» 패널 · `main.js` `data-team-export` 핸들러 · `home.js` ④ 이음새 절(→ «다음 할 일»만 남기거나 삭제) · `data.js` `loadExportStatus` · `state.js` `exportStatus` → `npm run build`
- [ ] **Step 5: 문서** — `연계계약_CONTRACT.md` §0 표 행 삭제·§3.4 를 §8 역사로 이동(JSON 규칙 전문은 git 이력 안내로 축약)·§5 명명 행 · `학습데이터_모델_관리.md` 의 `v2_model_sync` 절 · `다음_할_일_체크리스트.md` 37행 · `통합_인수인계_MASTER.md` 언급
- [ ] **Step 6: 통과 + 전체 회귀** (예상 283 − 4 − 10 − 5 + 부재 고정 ≈ 265~270, 실측으로 갱신) → 커밋

## Task 2: `HEAD /`·`HEAD /healthz` 405 → 200

- [ ] **Step 1: 테스트** — `test_http_hardening.py` 에 `client.head("/")`·`client.head("/healthz")` 200, `/` 는 `no-cache` 유지
- [ ] **Step 2: `server.py`** — 두 라우트를 `@app.api_route(..., methods=["GET","HEAD"])`
- [ ] **Step 3: 통과 → 커밋** (별건 칩 `task_a94e90a3` 은 dismiss)

## Task 3: 팀 문서 최신화

- [ ] `통합_인수인계_MASTER.md` — 상단 현행화 블록을 «Phase 6 완료» 기준으로 다시 쓰고, 본문에서 사실이 아닌 것(rev13 시절 라우트 수·테스트 수·폴더 지도·5번 탭 «자리 마련됨»)을 고친다. 전면 재작성은 하지 않는다(역사 가치) — 틀린 곳만
- [ ] `HANDOFF.md`·`HANDOFF_budget_app.md` — 상단에 «1단계 전용 역사 문서» 안내 한 블록
- [ ] `팀원_실행가이드.md` — 5번 탭·마감·기준연도 사용법, 첫 접속 지연 안내, v2 관련 절 삭제
- [ ] 패키지 `README.md` — 폴더 지도·«어디부터 읽나»에 `연계계약`·`사내이관_가이드`·공유 자료 추가
- [ ] `다음_할_일_체크리스트.md` — Phase 7 기준으로 «살아 있는 항목»만 남긴 새 절
- [ ] 커밋

## Task 4: 최종 공유 자료 — `docs/팀공유_최종안내.html`

- [ ] **Step 1: 캡처** — 로컬 uvicorn(실DB 사본, 2023·2025 마감) 을 내장 브라우저로 열어 홈 · 1 계획 · 2 실적 · 3 지사 · 4 통계 · 5 전망(3 하위 탭) · 설정 화면을 PNG 로. `docs/img/` 에 저장(총 16MB 이하)
- [ ] **Step 2: 본문** — ① 이게 무엇인가(한 문단 + 파이프라인 그림) ② 접속·로그인·첫 접속 지연 ③ 화면별 사용법(캡처 + 3~5줄) ④ 연도 운영 흐름(업로드 → 분석 → 확정 → 마감 → 전망) ⑤ 검증 현황(테스트 수·2023/2025 총액 일치·배포 실측) ⑥ **알려진 한계**(표본 2개년·계층적 후퇴 없음·정비등급 이력 비어 있음·Render 슬립·public 저장소) ⑦ 남은 일과 담당 ⑧ 문서 지도. 저장소 링크 없이 배포 주소만
- [ ] **Step 3: 검수** — 내장 브라우저로 열어 캡처·링크 확인, 모바일 폭에서 깨지지 않는지
- [ ] **Step 4: 커밋 · PR** — Task 1~4 를 한 PR 로(코드 변경은 Task 1·2 뿐)

## Task 5: 사용자 몫 (Claude 가 대신 못 하는 것)

- [ ] 병합 → Render **Manual Deploy** → `/healthz` commit 확인 (Task 1·2 는 코드 변경이라 배포 필요)
- [ ] 팀원 안내: 배포 주소 · 팀 비밀번호(비밀 메모) · 작업자 이름 규칙 · 공유 자료 HTML
- [ ] Supabase 2FA · Docker PC 에서 6-9 첫 실행 · 저장소 private 전환(보류 중)

## 완료 기준

| 항목 | 확인 |
|---|---|
| 죽은 코드 0 | `export-team`·`export_status`·`spec_io`·`v2_model_sync` 부재를 테스트가 고정 |
| 문서 = 사실 | `test_docs_consolidation` + 현행 문서에서 v2·Caddy 현재형 0 |
| 공유 자료 | HTML 한 장이 캡처와 함께 저장소 `docs/` 에 있고 배포 주소만 가리킴 |
| 회귀 | 전체 통과 |
