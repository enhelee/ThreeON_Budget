# 한난 디자인 시스템 적용 (Phase 8) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 예산·실적 분석 SPA 의 색·글꼴·형태·셸을 한난 디자인 시스템(공식 CI + 시안 규칙)으로 바꾸되 레이아웃·기능은 그대로 둔다.

**Architecture:** Tailwind 3.4 의 `theme.extend` 에 토큰 이름(`ink`·`sub`·`line`·`subtle`·`brand`·`ok/warn/err/info`)을 정의하고, `app.css` 의 공통 컴포넌트 클래스를 그 토큰으로 다시 쓴 뒤, 화면 10개의 옛 팔레트 유틸리티(`blue-*`·`slate-*`·`emerald-*`·`amber-*`·`red/rose-*`)를 치환 스크립트로 일괄 교체한다. 셸(사이드바·헤더·게이트)은 공식 시그니처 로고와 한난체 제목을 받고, 모바일 드로어에 Escape·포커스 복귀·포커스 가둠을 붙인다. 잔존 팔레트 0 을 테스트로 고정한다.

**Tech Stack:** Vite 6 · Tailwind 3.4.17(`@apply`) · Vanilla JS SPA · Python 3.14 + fontTools/brotli(폰트 서브셋) · pytest(구조 검사) · 내장 브라우저(실측)

**Spec:** [2026-09-21-한난-디자인시스템-design.md](../specs/2026-09-21-한난-디자인시스템-design.md)

## Global Constraints

- 작업 폴더 `2026-09-09-team-package-v0`. 브랜치 `phase8-hannan-ui`(생성됨, `edb37d5`). 테스트 `cd app && py -m pytest -q` — **기준선 269 passed**.
- 브랜드 빨강은 **`#FE0009`**(CI 매뉴얼 BS12 공식 RGB 254·0·9). 시안의 `#ED1C2B` 는 쓰지 않는다. 빨강 바탕에 흰 글자 금지(대비 4.0:1).
- 주요 버튼 = `#0D0D0D` 바탕 + 흰 글자 + `#FE0009` 1px 테두리 + 캡슐. 최소 높이 44px.
- 레이아웃(사이드바 288px·헤더·라우팅·메뉴 7개)·`custom.css` 의 상세표 열폭은 **바꾸지 않는다**. 색·글꼴·곡률·그림자·간격만.
- 프론트 수정 후 반드시 `cd app/web && npm run build`(산출물 `app/static/` 은 커밋 안 함).
- 로컬 확인 서버는 `.claude/launch.json` 의 `budget-local`(:8099, `DATABASE_URL=" "` 사본 DB). 배포본 로그인은 하지 않는다.
- 커밋 메시지 한국어, 본문에 «왜». 끝에 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Bash heredoc 은 백슬래시를 뭉갠다 — JS·CSS·파이썬 파일은 Write/Edit 도구로.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `app/web/tailwind.config.js` | 토큰 이름(colors·borderRadius·boxShadow·fontFamily) — 뷰의 유틸리티가 쓰는 이름 |
| `app/web/src/styles/app.css` | `:root` CSS 변수 · `@font-face` · base(포커스 링·본문 글꼴·reduced-motion) · 공통 컴포넌트 클래스 |
| `app/web/src/styles/custom.css` | 손으로 쓴 규칙 — 색만 CSS 변수로. 구조·열폭 불변 |
| `app/web/public/assets/kdhc-signature-ko.svg` | 공식 시그니처(시안 자산 복사) |
| `app/web/public/assets/fonts/HananCha-v1.woff2` | 한난체 서브셋 |
| `app/scripts/make_hanan_font.py` | TTF → 서브셋 woff2 (1회성, 원본 경로는 인자) |
| `app/scripts/restyle_sweep.py` | 옛 팔레트 → 토큰 치환표 실행 (1회성, 기록용으로 커밋) |
| `app/web/index.html` | 헤더 72px · 건너뛰기 링크 · 드로어 aria |
| `app/web/src/main.js:530-536` | 드로어 Escape·포커스 복귀·포커스 가둠 |
| `app/web/src/components/sidebar.js` | 로고 SVG · 메뉴 칩 색 |
| `app/web/src/components/badges.js` | 상태색 토큰 |
| `app/web/src/views/gate.js` | 로고 SVG · 강조색 |
| `app/web/src/views/*.js` | 치환 스크립트 결과 |
| `app/tests/test_ui_shell.py` | 구조 검사(토큰·폰트·로고·aria·잔존 팔레트 0) |
| `docs/UI_제작메모_한난.md` · `docs/img/ui/` | 시안 대비 변경·CI 근거·검증 결과·캡처 |

---

### Task 1: 토큰·글꼴·로고 자산 — 기반 만들기

**Files:**
- Modify: `app/web/tailwind.config.js`
- Modify: `app/web/src/styles/app.css:1-23` (base 층)
- Create: `app/scripts/make_hanan_font.py`
- Create: `app/web/public/assets/fonts/HananCha-v1.woff2` (생성물)
- Create: `app/web/public/assets/kdhc-signature-ko.svg` (복사)
- Test: `app/tests/test_ui_shell.py`

**Interfaces:**
- Produces: Tailwind 색 이름 `ink sub tri dis line line-strong line-subtle base subtle overlay brand brand-ink ok ok-bg warn warn-bg err err-bg info info-bg`, 곡률 `rounded-card rounded-visual rounded-control`, 그림자 `shadow-float`, 글꼴 `font-hanan`. CSS 변수 `--brand --ink --sub --tri --dis --line --line-strong --line-subtle --base --subtle --overlay --ok --ok-bg --warn --warn-bg --err --err-bg --info --info-bg --shadow-float --duration`. 이후 모든 태스크가 이 이름만 쓴다.

- [ ] **Step 1: 실패하는 테스트** — `app/tests/test_ui_shell.py` 신규

```python
# -*- coding: utf-8 -*-
"""한난 디자인 시스템 적용(Phase 8)의 구조 검사.

색·글꼴은 브라우저에서만 보이지만, «어느 이름이 정의돼 있는가»와 «옛 팔레트가 남았는가»는
파일만 읽어도 안다. 회귀는 조용히 온다 — 새 화면에 blue-600 을 한 줄 쓰면 아무 테스트도
안 깨지고 화면만 옛날로 돌아간다. 그래서 잔존 0 을 여기서 고정한다.
"""
import os
import re

APP = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
WEB = os.path.join(APP, "web")


def _read(*parts):
    with open(os.path.join(*parts), encoding="utf-8") as f:
        return f.read()


# ── Task 1: 토큰·글꼴·로고 ─────────────────────────────────────────────

def test_tailwind_defines_hannan_tokens():
    cfg = _read(WEB, "tailwind.config.js")
    for name in ("ink", "sub", "line", "subtle", "brand", "ok", "warn", "err", "info", "overlay"):
        assert re.search(rf"\b{name}\b\s*:", cfg), f"tailwind.config.js 에 색 토큰 «{name}» 이 없습니다"
    assert "#FE0009" in cfg.upper(), "브랜드 빨강은 CI 매뉴얼 공식값 #FE0009 여야 합니다"
    assert "#ED1C2B" not in cfg.upper(), "#ED1C2B 는 시안의 추출값 — 쓰지 않습니다"
    for r in ("card", "visual", "control"):
        assert re.search(rf"\b{r}\b\s*:", cfg), f"borderRadius «{r}» 이 없습니다"
    assert "hanan" in cfg, "fontFamily «hanan» 이 없습니다"


def test_css_root_declares_brand_and_font_face():
    css = _read(WEB, "src", "styles", "app.css")
    assert "--BRAND: #FE0009" in css.upper().replace("--BRAND:#", "--BRAND: #"), "app.css :root 의 --brand 는 #FE0009"
    assert "@font-face" in css and "HananCha" in css and "font-display: swap" in css
    assert "/assets/fonts/HananCha-v1.woff2" in css
    assert "prefers-reduced-motion" in css


def test_font_subset_and_logo_are_shipped():
    font = os.path.join(WEB, "public", "assets", "fonts", "HananCha-v1.woff2")
    assert os.path.isfile(font), "한난체 서브셋 woff2 가 없습니다 — py scripts/make_hanan_font.py <HANAN.TTF>"
    assert 50_000 < os.path.getsize(font) < 400_000, "서브셋 크기가 이상합니다(2,350자+ASCII 면 100~300KB)"
    with open(font, "rb") as f:
        assert f.read(4) == b"wOF2"
    svg = _read(WEB, "public", "assets", "kdhc-signature-ko.svg")
    assert "<svg" in svg and "viewBox" in svg
```

- [ ] **Step 2: 실패 확인** — `cd app && py -m pytest tests/test_ui_shell.py -q` → 3 failed

- [ ] **Step 3: `tailwind.config.js`**

```js
/** @type {import('tailwindcss').Config} */
// 한난 디자인 시스템 토큰(Phase 8). 값의 근거는 docs/superpowers/specs/2026-09-21-한난-디자인시스템-design.md §2·§3.
//   brand  = CI 매뉴얼 BS12 공식 RGB 254·0·9 (시안의 #ED1C2B 는 이미지 추출값 — 쓰지 않는다)
//   중성·상태색 = 시안 globals.css + openai-light.md 시맨틱
// 이름을 짧게 둔 이유: 뷰 10개에 수백 번 쓰인다. `text-ink` `bg-subtle` `border-line` 처럼 읽힌다.
export default {
  content: ["./index.html", "./src/**/*.{js,html}"],
  theme: {
    extend: {
      colors: {
        ink: "#0D0D0D",          // text-primary
        sub: "#5D5D5D",          // text-secondary (흰 배경 대비 6.58:1)
        tri: "#8F8F8F",          // text-tertiary (캡션·비활성 라벨)
        dis: "#A6A6A6",          // text-disabled
        line: { DEFAULT: "#E5E5E5", strong: "#C9C9C9", subtle: "#F2F2F2" },
        base: "#FFFFFF",
        subtle: "#F7F7F8",
        overlay: "rgba(13,13,13,0.56)",
        brand: { DEFAULT: "#FE0009", ink: "#000000" },
        ok: { DEFAULT: "#16794A", bg: "#EAF5EF" },
        warn: { DEFAULT: "#9A6700", bg: "#FFF4D6" },
        err: { DEFAULT: "#B42318", bg: "#FDECEC" },
        info: { DEFAULT: "#315EAC", bg: "#EAF2FF" },
      },
      borderRadius: { card: "16px", visual: "24px", control: "12px" },
      boxShadow: { float: "0 8px 24px rgba(0,0,0,0.10)" },
      fontFamily: {
        hanan: ['"HananCha"', "Pretendard", '"Apple SD Gothic Neo"', '"Malgun Gothic"', "system-ui", "sans-serif"],
        body: ["Pretendard", '"Apple SD Gothic Neo"', '"Malgun Gothic"', "system-ui", "sans-serif"],
      },
      transitionDuration: { DEFAULT: "180ms" },
    },
  },
  plugins: [],
}
```

- [ ] **Step 4: `app.css` base 층 교체** — 1~23행을 아래로

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ─────────────────────────────────────────────────────────────
   한난 디자인 시스템 (Phase 8, 2026-09-21)
   토큰의 근거: docs/superpowers/specs/2026-09-21-한난-디자인시스템-design.md §2·§3
   - 브랜드 빨강 #FE0009 = CI 매뉴얼 BS12 공식 RGB(254·0·9). 흰 글자 바탕으로 쓰지 않는다(대비 4.0:1).
   - 같은 값을 tailwind.config.js(유틸리티 이름)와 여기(CSS 변수)에 둔다 — custom.css 의
     손으로 쓴 규칙이 변수를 쓰기 때문. 값을 바꿀 땐 두 곳을 같이 바꾼다(test_ui_shell 이 본다).
   ───────────────────────────────────────────────────────────── */

@font-face {
  font-family: "HananCha";
  src: url("/assets/fonts/HananCha-v1.woff2") format("woff2");
  font-weight: 400 700;
  font-style: normal;
  font-display: swap;   /* 폰트가 늦어도 제목은 Pretendard 로 먼저 보인다 */
}

:root {
  --ink: #0D0D0D; --sub: #5D5D5D; --tri: #8F8F8F; --dis: #A6A6A6;
  --line: #E5E5E5; --line-strong: #C9C9C9; --line-subtle: #F2F2F2;
  --base: #FFFFFF; --subtle: #F7F7F8; --overlay: rgba(13,13,13,0.56);
  --brand: #FE0009; --brand-ink: #000000;
  --ok: #16794A; --ok-bg: #EAF5EF; --warn: #9A6700; --warn-bg: #FFF4D6;
  --err: #B42318; --err-bg: #FDECEC; --info: #315EAC; --info-bg: #EAF2FF;
  --shadow-float: 0 8px 24px rgba(0,0,0,0.10);
  --duration: 180ms;
}

@layer base {
  html {
    font-family: Pretendard, "Apple SD Gothic Neo", "Malgun Gothic", system-ui, sans-serif;
    word-break: keep-all;          /* 한글 제목·라벨은 의미 단위로 줄바꿈 */
    overflow-wrap: break-word;
  }
  body {
    @apply bg-base text-ink antialiased;
    font-size: 15px;
    line-height: 1.6;
  }
  /* 키보드 포커스는 브랜드 빨강 2px — 마우스 클릭에는 뜨지 않는다(:focus-visible) */
  :focus-visible {
    outline: 2px solid var(--brand);
    outline-offset: 3px;
  }
  button:focus:not(:focus-visible), input:focus:not(:focus-visible), select:focus:not(:focus-visible) {
    outline: none;
  }
  /* 숫자 표는 폭이 같은 숫자로 — 금액 열이 흔들리지 않는다 */
  td, th, .tabular { font-variant-numeric: tabular-nums; }
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
      scroll-behavior: auto !important;
    }
  }
}
```

(25행 이후 `@layer components` 는 Task 2 에서 다시 쓴다 — 이 단계에서는 그대로 둔다. `ring-blue-500` 이 남아 있어도 빌드는 된다.)

- [ ] **Step 5: 폰트 서브셋 스크립트** — `app/scripts/make_hanan_font.py`

```python
# -*- coding: utf-8 -*-
"""한난체(HananCha) TTF → 제목용 서브셋 woff2.

  py scripts/make_hanan_font.py "<HANAN.TTF 경로>" [출력=web/public/assets/fonts/HananCha-v1.woff2]

왜 서브셋인가: 원본 866KB. 제목에만 쓰므로 완성형 한글 2,350자 + ASCII + 기본 기호면 충분하다.
원본 TTF 는 CI 자산이라 저장소에 넣지 않는다 — 결과 woff2 만 커밋한다(사내 도구 자산).
파일명의 -v1 은 /assets/* 가 immutable 캐시라 교체 시 이름을 바꿔야 하기 때문이다.
"""
import os
import sys

from fontTools import subset

HERE = os.path.dirname(os.path.abspath(__file__))
DEFAULT_OUT = os.path.join(os.path.dirname(HERE), "web", "public", "assets", "fonts", "HananCha-v1.woff2")


def unicodes():
    u = set(range(0x20, 0x7F))                 # ASCII
    u |= set(range(0xAC00, 0xD7A4))            # 한글 음절 전체(폰트에 있는 2,350자만 남는다)
    u |= set(range(0x3131, 0x318F))            # 호환 자모
    u |= set(range(0x2000, 0x2070))            # 문장부호(–—‘’“”•…)
    u |= {0x00B7, 0x00D7, 0x00F7, 0x00B0, 0x00A0, 0x2190, 0x2192, 0x2191, 0x2193, 0x2264, 0x2265, 0x2260, 0x25A0, 0x25CF, 0x25CB, 0x3001, 0x3002, 0x300C, 0x300D, 0x3010, 0x3011, 0x00AB, 0x00BB}
    return sorted(u)


def main():
    if len(sys.argv) < 2:
        print(__doc__); return 2
    src, out = sys.argv[1], (sys.argv[2] if len(sys.argv) > 2 else DEFAULT_OUT)
    opts = subset.Options()
    opts.flavor = "woff2"
    opts.layout_features = ["*"]
    opts.name_IDs = ["*"]
    opts.notdef_outline = True
    font = subset.load_font(src, opts)
    sub = subset.Subsetter(opts)
    sub.populate(unicodes=unicodes())
    sub.subset(font)
    os.makedirs(os.path.dirname(out), exist_ok=True)
    subset.save_font(font, out, opts)
    print(f"{out}  {os.path.getsize(out)/1024:.0f} KB  glyphs={len(font.getGlyphOrder())}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 6: 자산 생성·복사**

```bash
cd 2026-09-09-team-package-v0/app
py scripts/make_hanan_font.py "C:/Users/User/Desktop/웹페이지 시안 만들기/이전/한난 CI/font/HANAN.TTF"
mkdir -p web/public/assets && cp "C:/Users/User/Desktop/웹페이지 시안 만들기/work/assets/kdhc-signature-ko.svg" web/public/assets/
```
기대: woff2 100~300KB, `glyphs=` 약 2,500.

- [ ] **Step 7: 통과 확인 + 빌드** — `py -m pytest tests/test_ui_shell.py -q` → 3 passed · `cd web && npm run build` 성공, `static/assets/fonts/HananCha-v1.woff2` 와 `static/assets/kdhc-signature-ko.svg` 존재

- [ ] **Step 8: 커밋**

```bash
git add app/web/tailwind.config.js app/web/src/styles/app.css app/scripts/make_hanan_font.py app/web/public app/tests/test_ui_shell.py
git commit -m "feat(ui): 한난 디자인 토큰·한난체 서브셋·공식 시그니처 자산 (Phase 8 Task 1)"
```

---

### Task 2: 공통 컴포넌트 재정의 (`app.css` components · `custom.css` 색)

**Files:**
- Modify: `app/web/src/styles/app.css` (`@layer components` 전체)
- Modify: `app/web/src/styles/custom.css` (색 값만)
- Test: `app/tests/test_ui_shell.py`

**Interfaces:**
- Consumes: Task 1 토큰
- Produces: 클래스 `.btn-primary .btn-secondary .btn-danger .btn-danger-outline .control .panel .metric-card .badge .section-title .section-help .table-head .table-cell .nav-button .nav-button-active .operator-badge` — 뷰가 이미 쓰는 이름 그대로(뷰 수정 없음)

- [ ] **Step 1: 실패하는 테스트** — `test_ui_shell.py` 에 추가

```python
# ── Task 2: 공통 컴포넌트 ────────────────────────────────────────────

def test_shared_css_uses_tokens_not_legacy_palette():
    """app.css·custom.css 에 옛 팔레트가 남으면 컴포넌트 절반이 파랑으로 돌아간다."""
    legacy = re.compile(r"\b(bg|text|border|ring|divide)-(blue|slate|emerald|amber|red|rose|indigo|gray)-\d+")
    for name in ("app.css", "custom.css"):
        body = _read(WEB, "src", "styles", name)
        hits = sorted(set(legacy.findall(body)))
        assert not hits, f"{name} 에 옛 팔레트 유틸리티: {hits}"
    custom = _read(WEB, "src", "styles", "custom.css")
    for hexv in ("#2563eb", "#1d4ed8", "#3730a3", "#eef2ff", "#dbeafe"):
        assert hexv not in custom.lower(), f"custom.css 에 옛 파랑 {hexv} 가 남아 있습니다"


def test_primary_button_follows_sample_rule():
    """검정 바탕 + 흰 글자 + 브랜드 빨강 테두리 + 캡슐 + 44px. 빨강 바탕은 금지."""
    css = _read(WEB, "src", "styles", "app.css")
    block = css.split(".btn-primary {", 1)[1].split("}", 1)[0]          # 단독 블록(색·테두리)
    assert "bg-ink" in block and "text-white" in block and "border-brand" in block
    shared = css.split(".btn-primary,", 1)[1].split("}", 1)[0]           # 공유 블록(형태)
    assert "rounded-full" in shared and "min-h-[44px]" in shared
    assert "bg-brand" not in block, "브랜드 빨강을 버튼 바탕으로 쓰면 흰 글자 대비가 4.5 미만입니다"
```

- [ ] **Step 2: 실패 확인** — `py -m pytest tests/test_ui_shell.py -q` → 2 failed

- [ ] **Step 3: `app.css` `@layer components` 전체 교체** (25행~끝)

```css
@layer components {
  /* 좌측 사이드바 — 활성은 검정 굵게 + 왼쪽 빨간 마커(시안의 활성 메뉴 밑줄을 세로 메뉴로) */
  .nav-button {
    @apply relative flex w-full min-h-[44px] items-center gap-3 rounded-control px-3 py-2.5 text-left text-sm font-medium text-sub transition;
  }
  .nav-button:hover {
    @apply bg-subtle text-ink;
  }
  .nav-button-active {
    @apply bg-subtle font-semibold text-ink;
  }
  .nav-button-active::before {
    content: "";
    @apply absolute left-0 top-2.5 bottom-2.5 w-0.5 rounded-full bg-brand;
  }
  .nav-button-active:hover {
    @apply bg-subtle text-ink;
  }

  /* 카드 — 16px 곡률, 1px 선, 그림자 없음(시안). 떠 있는 것만 shadow-float */
  .metric-card,
  .panel {
    @apply rounded-card border border-line bg-base;
  }
  .metric-card {
    @apply p-5;
  }

  /* 입력 — 포커스 시 검정 테두리(openai-light input 규칙). 오류는 err */
  .control {
    @apply h-10 rounded-control border border-line bg-base px-3 text-sm text-ink transition;
  }
  .control:focus {
    @apply border-ink outline-none;
    box-shadow: 0 0 0 1px var(--ink);
  }
  .control[aria-invalid="true"] {
    @apply border-err;
    box-shadow: 0 0 0 1px var(--err);
  }
  .control:disabled {
    @apply bg-subtle text-dis;
  }

  /* 버튼 — 캡슐, 최소 44px(터치), 시안 규칙: 주요=검정+흰+빨간 테두리, 보조=흰+strong 테두리 */
  .btn-primary,
  .btn-secondary {
    @apply inline-flex min-h-[44px] items-center justify-center gap-2 rounded-full px-5 py-2 text-sm font-medium transition;
  }
  .btn-primary:disabled,
  .btn-secondary:disabled {
    @apply cursor-not-allowed bg-subtle text-dis border-line;
  }
  .btn-primary {
    @apply border border-brand bg-ink text-white;
  }
  .btn-primary:hover:not(:disabled) {
    background-color: #333333;
  }
  .btn-secondary {
    @apply border border-line-strong bg-base text-ink;
  }
  .btn-secondary:hover:not(:disabled) {
    @apply border-ink bg-subtle;
  }

  /* 배지 — 상태색은 의미 전달용(정상·주의·오류·정보). 브랜드 빨강과 섞지 않는다 */
  .badge {
    @apply inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium;
  }

  .section-title {
    @apply font-hanan text-lg font-semibold tracking-tight text-ink;
  }
  .section-help {
    @apply mt-1 text-sm text-sub;
  }

  /* 표 — 열 폭 기준은 통합_인수인계_MASTER.md. 한글 헤더라 대문자 변환은 뺀다 */
  .table-head {
    @apply bg-subtle text-left text-xs font-semibold tracking-wide text-sub;
  }
  .table-cell {
    @apply whitespace-nowrap px-4 py-3 text-sm text-ink;
  }

  /* 헤더의 작업자 배지 — 중성색(옛 남색 제거) */
  .operator-badge {
    @apply inline-flex items-center gap-1.5 whitespace-nowrap rounded-full bg-subtle px-2.5 py-1 text-xs font-semibold text-ink;
  }
}
```

- [ ] **Step 4: `custom.css` 색 치환** — 구조는 그대로, 아래 값만 바꾼다(전체 파일에서 문자열 치환)

| 지금 | 이후 |
|---|---|
| `.login-overlay{…background:rgba(15,23,42,.58)` | `background:var(--overlay)` |
| `.login-card{…border-radius:1rem;…box-shadow:0 24px 60px rgba(15,23,42,.35)}` | `border-radius:16px;…box-shadow:var(--shadow-float)` |
| `.login-card h2{…color:#0f172a` · `.login-card p{…color:#64748b` · `.login-card label{…color:#475569` | `var(--ink)` · `var(--sub)` · `var(--sub)` |
| `.login-msg{…color:#b91c1c` | `var(--err)` |
| `.operator-badge{…}` 한 줄 | **삭제**(app.css 로 옮김) |
| `.audit-detail{…color:#64748b}` | `var(--sub)` |
| `.voucher-row td{background:#f8fafc;border-bottom:1px dashed #e2e8f0` | `var(--subtle)` · `var(--line)` |
| `.voucher-head td{background:#eef2f7;border-bottom:1px solid #e2e8f0` | `var(--line-subtle)` · `var(--line)` |
| `.row-new td{background:#fffbeb}` · `.row-missing td{background:#fef2f2}` · `.row-unmapped td{background:#f1f5f9}` · `.edit-row td{background:#f8fafc}` | `var(--warn-bg)` · `var(--err-bg)` · `var(--subtle)` · `var(--subtle)` |
| `.spinner{…border:3px solid #dbeafe;border-top-color:#2563eb` | `border:3px solid var(--line);border-top-color:var(--brand)` |
| `.btn-danger{…border-radius:.75rem;…background:#dc2626;…border:1px solid #dc2626;…box-shadow:0 1px 2px 0 rgba(0,0,0,.06)` | `border-radius:999px;…background:var(--err);…border:1px solid var(--err);…box-shadow:none;min-height:44px` |
| `.btn-danger:hover{background:#b91c1c;border-color:#b91c1c}` | `filter:brightness(.9)` 로 교체(`background`·`border-color` 삭제) |
| `.btn-danger-outline{…border-radius:.75rem;…color:#dc2626;border:1px solid #fca5a5` | `border-radius:999px;…color:var(--err);border:1px solid var(--err);min-height:44px` |
| `.btn-danger-outline:hover{background:#fef2f2}` · `.danger-box{border:1px solid #fca5a5;background:#fef2f2;border-radius:.75rem` | `var(--err-bg)` · `border:1px solid var(--err);background:var(--err-bg);border-radius:12px` |
| `.danger-title{color:#991b1b` · `.danger-text{color:#b91c1c` · `.danger-label{color:#b91c1c` | 모두 `var(--err)` |
| `.pending-count{…color:#1d4ed8` | `color:var(--ink)` |
| `.btn-pending{background:#b45309 !important;border-color:#b45309 !important;` · `.btn-pending .pending-count{color:#b45309}` · `pendPulse` 의 `rgba(180,83,9,…)` | `var(--warn)` 로 (색은 의미색 유지) |
| `.pending-bar{…border:1px solid #fcd34d;background:#fffbeb;border-radius:.75rem` · `.pending-bar b{color:#92400e}` | `border:1px solid var(--warn);background:var(--warn-bg);border-radius:12px` · `var(--warn)` |
| `.swatch{…border:1px solid #e2e8f0` | `var(--line)` |

- [ ] **Step 5: 통과 + 빌드** — `py -m pytest tests/test_ui_shell.py -q` → 5 passed · `npm run build` 성공
- [ ] **Step 6: 커밋** — `git commit -m "feat(ui): 공통 컴포넌트를 한난 토큰으로 — 캡슐 버튼·16px 카드·상태색 배지 (Task 2)"`

---

### Task 3: 셸 — 헤더·사이드바·게이트·모바일 드로어 접근성

**Files:**
- Modify: `app/web/index.html:11-62`
- Modify: `app/web/src/components/sidebar.js:38-69`
- Modify: `app/web/src/views/gate.js:18-50`
- Modify: `app/web/src/main.js:530-536`
- Test: `app/tests/test_ui_shell.py`

**Interfaces:**
- Consumes: Task 1·2
- Produces: `openMenu()`/`closeMenu()`(기존 export 유지, `closeMenu` 는 포커스를 `#menuButton` 으로 돌린다) · `#menuButton[aria-expanded]` · 건너뛰기 링크 `a.skip-link[href="#page"]`

- [ ] **Step 1: 실패하는 테스트**

```python
# ── Task 3: 셸 ────────────────────────────────────────────────────────

def test_shell_has_skip_link_header_height_and_drawer_aria():
    html = _read(WEB, "index.html")
    assert 'lang="ko"' in html
    assert 'class="skip-link"' in html and 'href="#page"' in html and "본문 바로가기" in html
    assert "min-h-[72px]" in html, "헤더는 72px(시안)"
    assert 'aria-expanded="false"' in html and 'aria-controls="sidebar"' in html
    assert "backdrop-blur" not in html, "시안 헤더는 불투명 흰색"


def test_sidebar_and_gate_use_official_signature():
    for f in (os.path.join(WEB, "src", "components", "sidebar.js"), os.path.join(WEB, "src", "views", "gate.js")):
        body = _read(f)
        assert "/assets/kdhc-signature-ko.svg" in body, f"{os.path.basename(f)}: 공식 시그니처 SVG"
        assert 'alt="한국지역난방공사"' in body, f"{os.path.basename(f)}: 로고 대체 텍스트"


def test_drawer_handles_escape_and_returns_focus():
    js = _read(WEB, "src", "main.js")
    assert '"Escape"' in js, "드로어는 Escape 로 닫혀야 합니다"
    assert "aria-expanded" in js
    assert "menuReturnEl" in js and ".focus()" in js, "닫힌 뒤 메뉴 버튼으로 포커스가 돌아와야 합니다"
    assert '"Tab"' in js, "열려 있는 동안 Tab 은 드로어 안에서 순환"
```

- [ ] **Step 2: 실패 확인** → 3 failed

- [ ] **Step 3: `index.html` 11~62행 교체**

```html
  <a class="skip-link" href="#page">본문 바로가기</a>

  <div id="appShell" class="min-h-screen lg:flex">
    <div id="backdrop" class="fixed inset-0 z-30 hidden bg-overlay lg:hidden" aria-hidden="true"></div>

    <aside id="sidebar" aria-label="주요 메뉴" class="fixed inset-y-0 left-0 z-40 flex w-72 -translate-x-full flex-col border-r border-line bg-base px-4 py-5 transition-transform lg:sticky lg:top-0 lg:h-screen lg:translate-x-0">
      <!-- 내용은 components/sidebar.js 가 채운다 (main.js 부팅 시 1회) -->
    </aside>

    <div class="min-w-0 flex-1">
      <header class="sticky top-0 z-20 border-b border-line bg-base">
        <div class="flex min-h-[72px] items-center justify-between gap-4 px-4 sm:px-6 lg:px-8">
          <div class="flex min-w-0 items-center gap-3">
            <button id="menuButton" class="grid h-11 w-11 place-items-center rounded-full border border-line-strong text-ink lg:hidden" aria-label="전체 메뉴 열기" aria-expanded="false" aria-controls="sidebar">
              <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
            </button>
            <div class="min-w-0">
              <h1 id="pageTitle" class="truncate font-hanan text-[22px] font-semibold tracking-tight text-ink">홈</h1>
              <p id="pageSubtitle" class="hidden truncate text-xs text-sub sm:block"></p>
            </div>
          </div>
          <div class="flex items-center gap-2">
            <label class="hidden items-center gap-2 text-xs font-semibold text-sub sm:flex">대상연도
              <select id="yearSelect" class="control"></select>
            </label>
            <button id="addYearBtn" class="btn-secondary hidden sm:inline-flex" title="새 연도 추가">＋연도</button>
            <button class="btn-primary" data-action="analyze" id="analyzeBtn">
              <svg viewBox="0 0 24 24" class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M13 2 4 14h6l-1 8 9-12h-6l1-8z"/></svg>
              <span id="analyzeLabel">분석 실행</span><span id="pendingCount" class="pending-count hidden"></span>
            </button>
            <span id="operatorBadge" class="operator-badge hidden" title="로그인 때 입력한 작업자 이름 — 모든 변경이 이 이름으로 기록됩니다"></span>
            <button id="logoutBtn" class="btn-secondary hidden" data-action="logout">로그아웃</button>
          </div>
        </div>
        <div id="lockBanner"></div>
      </header>

      <main id="page" class="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8" aria-live="polite" tabindex="-1"></main>
    </div>
  </div>

  <!-- 로그인 게이트 — 비로그인 시 appShell 을 대신해 화면 전체를 차지한다.
       내용은 views/gate.js 가 채운다. -->
  <div id="gate" hidden></div>

  <input type="file" id="trainingFile" accept=".csv" class="hidden">

  <div id="toast" class="pointer-events-none fixed bottom-5 right-5 z-50 hidden max-w-sm rounded-visual bg-ink px-4 py-3 text-sm font-medium text-white shadow-float" role="status"></div>
  <div id="busy" class="fixed inset-0 z-50 hidden place-items-center bg-overlay">
    <div class="rounded-visual bg-base px-8 py-6 text-center shadow-float">
      <div class="spinner mx-auto"></div>
      <p id="busyText" class="mt-3 text-sm font-semibold text-ink">처리 중...</p>
    </div>
  </div>
```

그리고 `custom.css` 끝에 건너뛰기 링크:

```css
/* 건너뛰기 링크 — 포커스될 때만 보인다(시안 .skip-link) */
.skip-link{position:fixed;top:-90px;left:20px;z-index:200;padding:12px 20px;background:var(--ink);color:#fff;border-radius:0 0 8px 8px;font-size:14px}
.skip-link:focus{top:0}
```

- [ ] **Step 4: `sidebar.js` 38~69행 교체**

```js
function navButton(m) {
  return `<button class="nav-button" data-view="${m.view}"><span class="grid h-7 w-7 place-items-center rounded-lg bg-subtle text-xs text-sub">${esc(m.tag)}</span>${esc(m.label)}</button>`
}

function groupLabel(text) {
  return `<p class="px-3 pb-1 pt-5 text-xs font-semibold tracking-wider text-tri">${esc(text)}</p>`
}

/** <aside id="sidebar"> 의 내용. 바깥 껍데기(위치·모바일 토글)는 index.html 이 갖는다. */
export function renderSidebar() {
  return `
    <div class="px-2">
      <img src="/assets/kdhc-signature-ko.svg" alt="한국지역난방공사" width="184" height="41" class="block h-auto w-[184px]">
      <p class="mt-3 text-[13px] leading-5 text-sub">예산·실적 분석 · 발전플랜트 유지보수</p>
    </div>

    <nav class="mt-8 flex-1 space-y-1" aria-label="주요 메뉴">
      ${MENU.map(m => m.group ? groupLabel(m.group) : navButton(m)).join("\n      ")}
    </nav>

    <div class="rounded-card border border-line bg-subtle p-4">
      <div class="flex items-center gap-2 text-sm font-semibold text-ink">
        <span class="h-2 w-2 rounded-full bg-ok" aria-hidden="true"></span>사내망 · 백엔드 DB 저장
      </div>
      <p class="mt-2 text-xs leading-5 text-sub">업로드 자료는 서버 DB에만 저장되고 원본 파일은 남지 않습니다. 화면에는 분석 결과만 표출됩니다.</p>
    </div>`
}
```

(`height="41"`: SVG 비율은 로고 8.317:1 + 위아래 보호 여백 0.6h → 184px 폭에서 약 41px. 빌드 후 실측해 맞춘다.)

- [ ] **Step 5: `gate.js` 18~50행 교체**

```js
const STAGES = ["예산 계획", "실적 집계·매칭", "예산 표준화", "중장기 전망"]

function stageFlow() {
  return STAGES.map((s, i) => `
    <span class="inline-flex items-center gap-1.5">
      <span class="grid h-5 w-5 place-items-center rounded-md bg-subtle text-[0.65rem] font-bold text-ink">${i + 1}</span>
      <span class="text-xs font-medium text-sub">${s}</span>
    </span>
    ${i < STAGES.length - 1 ? '<span class="text-tri" aria-hidden="true">→</span>' : ""}`).join("")
}

export function renderGate() {
  return `
  <div class="flex min-h-screen items-center justify-center bg-base px-4 py-10">
    <div class="panel w-full max-w-md p-7 sm:p-9">

      <img src="/assets/kdhc-signature-ko.svg" alt="한국지역난방공사" width="184" height="41" class="block h-auto w-[184px]">
      <p class="mt-3 text-[13px] text-sub">예산·실적 분석 · 발전플랜트 유지보수</p>

      <h1 class="mt-7 font-hanan text-[28px] font-semibold leading-snug tracking-tight text-ink">
        계획에서 실적,<br>실적에서 10개년 소요 전망까지
      </h1>
      <p class="section-help leading-6">
        지사 19곳의 예산계획과 SAP 실적 전표를 자동으로 맞춰 보고,
        그 결과를 정비등급별 표준금액과 2026~2035년 예산 전망의 근거로 넘깁니다.
      </p>

      <div class="mt-5 flex flex-wrap items-center gap-x-2 gap-y-2 border-y border-line py-3">
        ${stageFlow()}
      </div>
```
(폼 부분 51행~ 은 Task 4 의 치환 스크립트가 처리한다.)

- [ ] **Step 6: `main.js` 530~536행 교체 — 드로어 접근성**

```js
// ── 모바일 드로어 — Escape 닫기 · 닫은 뒤 메뉴 버튼으로 포커스 복귀 · 열린 동안 Tab 순환 ──
// (시안의 Radix Sheet 가 하던 일을 손으로 한다. 데스크톱(lg 이상)에서는 사이드바가 고정이라 해당 없음.)
const FOCUSABLE = 'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])';
let menuReturnEl = null;   // 열 때 포커스가 있던 요소 — 보통 #menuButton

function drawerIsOpen() {
  return window.innerWidth < 1024 && !$("#sidebar").classList.contains("-translate-x-full");
}

export function openMenu() {
  const sb = $("#sidebar");
  menuReturnEl = document.activeElement;
  sb.classList.remove("-translate-x-full"); $("#backdrop").classList.remove("hidden");
  $("#menuButton").setAttribute("aria-expanded", "true");
  sb.setAttribute("role", "dialog"); sb.setAttribute("aria-modal", "true");
  const first = sb.querySelector(FOCUSABLE);
  if (first) first.focus();
}

export function closeMenu() {
  if (window.innerWidth >= 1024) return;
  const sb = $("#sidebar");
  if (sb.classList.contains("-translate-x-full")) return;
  sb.classList.add("-translate-x-full"); $("#backdrop").classList.add("hidden");
  $("#menuButton").setAttribute("aria-expanded", "false");
  sb.removeAttribute("role"); sb.removeAttribute("aria-modal");
  const back = (menuReturnEl && document.contains(menuReturnEl)) ? menuReturnEl : $("#menuButton");
  back.focus();
}

document.addEventListener("keydown", e => {
  if (!drawerIsOpen()) return;
  if (e.key === "Escape") { e.preventDefault(); closeMenu(); return; }
  if (e.key === "Tab") {
    const items = [...$("#sidebar").querySelectorAll(FOCUSABLE)].filter(el => el.offsetParent !== null);
    if (!items.length) return;
    const first = items[0], last = items[items.length - 1];
    if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
    else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
  }
});

$("#menuButton").addEventListener("click", openMenu);

$("#backdrop").addEventListener("click", closeMenu);
```

- [ ] **Step 7: 통과 + 빌드** — `py -m pytest tests/test_ui_shell.py -q` → 8 passed · `npm run build`
- [ ] **Step 8: 로컬 실측(드로어)** — `budget-local` 서버, 내장 브라우저 390px: 메뉴 버튼 → 드로어 열림·첫 항목 포커스 → Tab 끝에서 처음으로 → Escape → 닫히고 `document.activeElement.id === "menuButton"` (`javascript_tool` 로 확인)
- [ ] **Step 9: 커밋** — `git commit -m "feat(ui): 셸 — 공식 시그니처 로고·72px 헤더·건너뛰기 링크·드로어 Escape/포커스 복귀 (Task 3)"`

---

### Task 4: 화면 10개 색 유틸리티 일괄 치환

**Files:**
- Create: `app/scripts/restyle_sweep.py`
- Modify: `app/web/src/views/*.js` · `app/web/src/components/badges.js` · `app/web/src/components/*.js` · `app/web/src/main.js` · `app/web/src/forecast_actions.js` (스크립트 결과)
- Test: `app/tests/test_ui_shell.py`

**Interfaces:**
- Consumes: Task 1 토큰 이름
- Produces: 소스 전체에 옛 팔레트 0

- [ ] **Step 1: 실패하는 테스트**

```python
# ── Task 4: 옛 팔레트 잔존 0 ─────────────────────────────────────────

LEGACY = re.compile(r"\b(?:hover:|focus:|sm:|lg:|md:)?(?:bg|text|border|ring|accent|divide|from|to)-(?:blue|slate|emerald|amber|red|rose|indigo|gray|green|yellow)-\d+(?:/\d+)?\b")


def test_no_legacy_palette_anywhere_in_frontend_source():
    """새 화면에 blue-600 한 줄이 들어와도 아무 테스트도 안 깨진다 — 여기서 깨진다."""
    hits = {}
    for root, _, files in os.walk(os.path.join(WEB, "src")):
        for f in files:
            if f.endswith(".js"):
                found = sorted(set(LEGACY.findall(_read(root, f))))
                if found:
                    hits[f] = found
    found = sorted(set(LEGACY.findall(_read(WEB, "index.html"))))
    if found:
        hits["index.html"] = found
    assert not hits, "옛 팔레트 유틸리티가 남아 있습니다: " + repr(hits)


def test_no_card_shadows_left():
    """시안: 일반 카드는 그림자 없음. 떠 있는 것(토스트·바쁨)만 shadow-float."""
    src = ""
    for root, _, files in os.walk(os.path.join(WEB, "src")):
        for f in files:
            if f.endswith(".js"):
                src += _read(root, f)
    src += _read(WEB, "index.html")
    assert not re.search(r"\bshadow-(sm|md|lg|xl)\b|\bshadow\b(?!-float)", src), "shadow-* 유틸리티가 남아 있습니다"
```

- [ ] **Step 2: 실패 확인** → 2 failed (잔존 목록이 메시지에 찍힌다 — 그것이 치환표의 검산이다)

- [ ] **Step 3: 치환 스크립트** — `app/scripts/restyle_sweep.py`

```python
# -*- coding: utf-8 -*-
"""옛 팔레트 유틸리티 → 한난 토큰 일괄 치환 (Phase 8 Task 4, 1회성 — 기록용으로 남긴다).

  py scripts/restyle_sweep.py            # 실행(덮어씀)
  py scripts/restyle_sweep.py --dry-run  # 파일별 치환 건수만

치환표의 근거: docs/superpowers/specs/2026-09-21-한난-디자인시스템-design.md §7.
순서가 중요하다 — 긴 토큰(`bg-slate-950/30`)을 짧은 것(`bg-slate-950`)보다 먼저 바꾼다.
`hover:`·`sm:` 같은 변형 접두는 토큰 앞에 붙어 있으므로 본체만 바꾸면 그대로 따라온다.
"""
import os
import re
import sys

WEB = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "web")

RULES = [
    # 오버레이·검정 면
    ("bg-slate-950/30", "bg-overlay"), ("bg-slate-950", "bg-ink"),
    # 파랑(강조) → 검정/중성. 호버 글자만 브랜드 빨강
    ("hover:text-blue-900", "hover:text-brand"), ("hover:text-blue-700", "hover:text-brand"),
    ("hover:bg-blue-50/40", "hover:bg-subtle"), ("hover:bg-blue-100", "hover:bg-subtle"),
    ("hover:border-blue-400", "hover:border-ink"), ("hover:border-blue-300", "hover:border-ink"),
    ("text-blue-950", "text-ink"), ("text-blue-900", "text-ink"), ("text-blue-800", "text-ink"), ("text-blue-700", "text-ink"),
    ("bg-blue-700", "bg-ink"), ("bg-blue-600", "bg-ink"), ("bg-blue-500", "bg-ink"),
    ("bg-blue-100", "bg-subtle"), ("bg-blue-50", "bg-subtle"),
    ("border-blue-300", "border-line-strong"), ("border-blue-200", "border-line-strong"),
    ("accent-blue-600", "accent-brand"), ("ring-blue-500", "ring-brand"),
    # 중성 slate → 4단계
    ("text-slate-950", "text-ink"), ("text-slate-900", "text-ink"), ("text-slate-800", "text-ink"), ("text-slate-700", "text-ink"),
    ("text-slate-600", "text-sub"), ("text-slate-500", "text-sub"),
    ("text-slate-400", "text-tri"), ("text-slate-300", "text-tri"),
    ("hover:bg-slate-50", "hover:bg-subtle"),
    ("bg-slate-300", "bg-line-strong"), ("bg-slate-200", "bg-line"), ("bg-slate-100", "bg-subtle"), ("bg-slate-50", "bg-subtle"),
    ("border-slate-300", "border-line-strong"), ("border-slate-200", "border-line"),
    ("divide-slate-100", "divide-line-subtle"),
    # 상태색 — 의미 유지
    ("text-emerald-700", "text-ok"), ("bg-emerald-500", "bg-ok"), ("bg-emerald-100", "bg-ok-bg"), ("bg-emerald-50", "bg-ok-bg"), ("border-emerald-200", "border-ok"),
    ("text-amber-900", "text-warn"), ("text-amber-800", "text-warn"), ("text-amber-700", "text-warn"),
    ("bg-amber-500", "bg-warn"), ("bg-amber-50", "bg-warn-bg"), ("border-amber-300", "border-warn"), ("border-amber-200", "border-warn"),
    ("text-red-600", "text-err"), ("text-rose-900", "text-err"), ("text-rose-800", "text-err"), ("text-rose-700", "text-err"),
    ("bg-rose-50", "bg-err-bg"), ("border-rose-300", "border-err"),
    # 형태 — 카드 16 · 입력/컨테이너 12 · 그림자 제거(떠 있는 것은 index.html 이 shadow-float 로 직접 지정)
    ("rounded-2xl", "rounded-card"), ("rounded-xl", "rounded-control"),
    ("hover:shadow-md", ""), ("shadow-xl", "shadow-float"), ("shadow-lg", "shadow-float"), ("shadow-sm", ""),
]
_SHADOW_BARE = re.compile(r"(?<![\w-])shadow(?![\w-])")   # 낱말 `shadow` 만


def sweep(text):
    n = 0
    for old, new in RULES:
        c = text.count(old)
        if c:
            text = text.replace(old, new); n += c
    text, c = _SHADOW_BARE.subn("", text); n += c
    text = re.sub(r'class="([^"]*)"', lambda m: 'class="' + " ".join(m.group(1).split()) + '"', text)   # 빈칸 정리
    return text, n


def main():
    dry = "--dry-run" in sys.argv
    total = 0
    targets = [os.path.join(WEB, "index.html")]
    for root, _, files in os.walk(os.path.join(WEB, "src")):
        targets += [os.path.join(root, f) for f in files if f.endswith(".js")]
    for p in targets:
        with open(p, encoding="utf-8") as f:
            s = f.read()
        t, n = sweep(s)
        if n:
            print(f"{os.path.relpath(p, WEB):40s} {n:4d}")
            total += n
            if not dry:
                with open(p, "w", encoding="utf-8", newline="\n") as f:
                    f.write(t)
    print("합계", total, "(dry-run)" if dry else "")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 실행** — `py scripts/restyle_sweep.py --dry-run` 으로 건수 확인 → `py scripts/restyle_sweep.py` 실행. 이후 `badges.js` 는 손으로 의미를 맞춘다:

```js
export function rateBadge(p, a) {
  const r = rateVal(p, a);
  if (r === null) return '<span class="badge bg-subtle text-sub">-</span>';
  const cls = r >= 95 ? "bg-info-bg text-info" : r >= 85 ? "bg-ok-bg text-ok" : "bg-warn-bg text-warn";
  return `<span class="badge ${cls}">${r.toFixed(1)}%</span>`;
}

export function confBadge(c) {
  if (c === null || c === undefined) return '<span class="badge bg-subtle text-sub">신규</span>';
  const n = Number(c);
  const label = n >= 0.99 ? (n >= 1 ? "수동 1.00" : "학습 0.99") : n.toFixed(2);
  const cls = n >= 0.8 ? "bg-ok-bg text-ok" : "bg-warn-bg text-warn";
  return `<span class="badge ${cls}">${label}</span>`;
}

export function gubunBadge(g) {
  const map = {"계획집행": "bg-info-bg text-info", "미시행": "bg-subtle text-sub",
               "신규": "bg-warn-bg text-warn", "신규(소액집행)": "bg-warn-bg text-warn",
               "미매핑": "bg-err-bg text-err"};
  return `<span class="badge ${map[g] || "bg-subtle text-sub"}">${esc(g)}</span>`;
}
```

- [ ] **Step 5: 통과 + 빌드** — `py -m pytest tests/test_ui_shell.py -q` → 10 passed · `npm run build` · 빌드 CSS 에서도 `grep -c "blue-\|slate-" static/assets/index-*.css` → 0
- [ ] **Step 6: 눈으로 보는 예외(설계서 §7 끝)** — `budget-local` 1440px: 홈(«이 연도로 전환 →» 링크가 검정+호버 빨강인지) · 3 지사 표(집행률 배지: 95%↑ 파랑 info, 85~95 초록, 미만 주황) · 3+ 상세(상태 배지·`det-table` 열폭 그대로) · 5 전망(하위 탭 버튼 활성 = 검정 캡슐) · 설정(감사 로그 표). 어색한 곳은 **그 한 곳만** 손으로 고치고 커밋 메시지에 적는다.
- [ ] **Step 7: 전체 회귀** — `py -m pytest -q` → 269 + 10 = 279
- [ ] **Step 8: 커밋** — `git commit -m "feat(ui): 화면 10개 색 유틸리티를 한난 토큰으로 일괄 치환 — 잔존 0 고정 (Task 4)"`

---

### Task 5: 4개 폭 실측·접근성 검증 · 제작 메모 · 캡처 · PR

**Files:**
- Create: `docs/UI_제작메모_한난.md`
- Create: `docs/img/ui/*.png` (1440·768·390·360 × 대표 화면)
- Modify: `docs/팀공유_최종안내.html` 의 캡처(`docs/img/share/*.png`) — 새 화면으로 재캡처
- Modify: `docs/superpowers/plans/2026-09-21-Phase8-한난-디자인시스템.md` 체크

**Interfaces:** — 없음(검증·문서)

- [ ] **Step 1: 로컬 서버** — `preview_start budget-local`(사본 DB, 2023·2025 마감돼 있음)
- [ ] **Step 2: 폭별 실측** — 내장 브라우저 `resize_window` 1440·768·390·360 에서 홈·3 지사·3+ 상세·5 전망(3탭)·설정 각각: `javascript_tool` 로 `document.documentElement.scrollWidth > innerWidth` 가 `false`, 캡처(`docs/img/ui/{폭}-{화면}.png`). 넘침·잘림·겹침이 있으면 해당 뷰의 유틸리티(`min-w-0`·`overflow-x-auto`·`flex-wrap`)로 고치고 다시 잰다. `det-table` 은 설계상 `min-width:56rem` 가로 스크롤이 정상.
- [ ] **Step 3: 키보드·드로어·모션** — 390px: Tab 으로 건너뛰기 링크 → 메뉴 버튼 → 드로어 열기 → Tab 순환 → Escape → 포커스 복귀 확인. `resize_window` 로 `prefers-reduced-motion` 은 에뮬레이션이 없으므로 코드 검토(`app.css` 의 미디어쿼리)로 표시.
- [ ] **Step 4: 대비 계산** — 토큰 조합 8개를 파이썬으로 계산해 표로(WCAG 상대 휘도): `ink/base` · `sub/base` · `sub/subtle` · `tri/base`(캡션, 3.5 예상 → «큰 글자·비활성 라벨 한정» 명시) · `white/ink` · `ok/ok-bg` · `warn/warn-bg` · `err/err-bg` · `info/info-bg` · `white/brand`(4.0 — 사용 안 함 근거)
- [ ] **Step 5: `docs/UI_제작메모_한난.md`** — ① 시안 대비 변경(레이아웃 유지·색·글꼴·형태·셸) ② CI 사용 근거(공식값 `#FE0009` vs 추출값 `#ED1C2B`, 시그니처 출처, 한난체 출처·라이선스 미확인) ③ 검증 결과 — **실측한 것**(폭별 넘침·드로어·포커스·캡처)과 **코드 검토만 한 것**(모션 감소·대비 계산) 구분 ④ 남은 항목(사용자 확인: 한난체 사용 범위, 팀 피드백 반영)
- [ ] **Step 6: 공유 안내 캡처 갱신** — Edge 헤드리스로 `docs/img/share/*.png` 10장 재캡처(Phase 7 절차와 동일: `--headless=new --screenshot --virtual-time-budget=40000`, 한글 URL 은 퍼센트 인코딩)
- [ ] **Step 7: 전체 회귀 + 커밋 + PR** — 279 passed → `git commit -m "docs(ui): 한난 디자인 제작 메모·4개 폭 실측 캡처·공유 안내 캡처 갱신 (Task 5)"` → `gh pr create --body-file`(본문: 결정·변경·CI 근거·실측/코드검토 구분·병합 후 Manual Deploy)
- [ ] **Step 8: 병합 후(사용자)** — Manual Deploy → `/healthz` commit · 번들 해시 · `/assets/kdhc-signature-ko.svg` 200 · `/assets/fonts/HananCha-v1.woff2` 200(immutable)

---

## 완료 기준

| 항목 | 확인 |
|---|---|
| 옛 팔레트·그림자 잔존 0 | `test_ui_shell.py` 10개 |
| 브랜드 규칙 | `btn-primary` = ink+white+brand 테두리, 빨강 바탕 없음(테스트) |
| 접근성 | 건너뛰기 링크 · `:focus-visible` 빨간 링 · 드로어 Escape/포커스 복귀/Tab 순환(실측) · 로고 alt · 44px |
| 반응형 | 1440·768·390·360 가로 넘침 0(실측 캡처) |
| 회귀 | 279 passed · 레이아웃·기능 불변 |
| 문서 | 제작 메모(실측/코드검토 구분) · 공유 안내 캡처 갱신 |
