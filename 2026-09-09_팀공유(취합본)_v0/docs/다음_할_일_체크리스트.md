# 다음 할 일 체크리스트 — 무엇부터, 누가 (2026-09-09)

> 아래 순서대로 하면 됩니다. **[사용자]** = 계정·비밀값이 필요해 사용자만 할 수 있는 일, **[Claude]** = 그 결과를 받아 이어서 처리할 일.
> 현재까지 검증된 것: budget_app 테스트 87 · forecast_app 183 · PostgreSQL 실서버 워크로드 · Caddy 한 주소 라우팅. 미검증: Docker 이미지 빌드(Render 첫 배포에서 확인), Supabase 실접속.

## 0단계 — 오늘 바로 (10분)
- [x] **[사용자]** `budget_app/.env`의 `APP_PASSWORD`를 팀 비밀번호로 바꾼다(초기값 `threeon-2026`). 12자 이상 권장. `SECRET_KEY`는 이미 무작위 생성됨.
- [x] **[사용자]** 로컬에서 한 번 열어 본다: `budget_app/분석프로그램_실행.bat` → 로그인(작업자 이름+비밀번호) → 설정 탭 「변경 이력」·「분류 모델 레지스트리」 확인.

## 1단계 — GitHub 업로드 (20분)
- [x] **[사용자]** `2026-09-09_팀공유(취합본)_v0/` 폴더를 `enhelee/ThreeON_Budget` 저장소에 같은 이름의 새 폴더로 올린다.
  - 올리기 전 폴더 안에 `.env`·`*.db`·`data_*.csv`·`checkpoint_data/`가 없는지 확인(패키지 생성 시 유출 검사 통과, `.gitignore` 포함).
  - 저장소 **루트**에 `render.yaml`을 복사하고 두 줄만 고친다: `dockerContext: ./2026-09-09_팀공유(취합본)_v0`, `dockerfilePath: ./2026-09-09_팀공유(취합본)_v0/Dockerfile`.
- [ ] **[사용자]** (나중에) GitHub Pages: Settings → Pages → Deploy from a branch → 폴더 `/docs`. 저장소 루트 `docs/`에 패키지의 `docs/index.html`을 복사해 두면 소개 사이트가 뜬다(또는 Vercel Root Directory=`2026-09-09_팀공유(취합본)_v0/docs`).
- [x] **[Claude]** 업로드된 저장소 구조 점검 완료(2026-09-09): 202개 파일 전부 일치·유출 0. 루트 `render.yaml`은 Claude가 커밋(`a3162ac`) — 경로를 따옴표로 감싼 `"./2026-09-09_팀공유(취합본)_v0"`. Pages는 보류(Render `/`가 같은 소개 사이트를 제공).
- [ ] **[사용자]** (선택) 저장소가 **public** 상태 — 루트 `data_2016.csv`·`training_data.csv`·`classified_2016.csv`에 실데이터가 있으면 private로 전환.

## 2단계 — Supabase (15분)
- [x] **[사용자]** supabase.com 가입 → New project(리전 Seoul) → DB 비밀번호 생성·보관(비밀 메모).
- [x] **[사용자]** ~~Project Settings → Database~~ → **상단 초록 `Connect` 버튼 → 3번째 카드 `Direct/Connection string`** → Connection string → **Session pooler(5432)** 복사, 끝에 `?sslmode=require`.
- [x] **[사용자]** 로컬 `budget_app/.env`에 `DATABASE_URL=…` 한 줄 추가. 함정 2개(자리표시자 대괄호 `[ ]` 제거, 특수문자 퍼센트 인코딩)는 Claude가 교정.
- [ ] **[사용자]** Supabase 계정 2단계 인증 켜기.
- [x] **[Claude]** 완료(2026-09-10): 스키마 18표 생성·쓰기OK → 정리(187.9MB→19.8MB, run 89건·match_line 127만행 삭제, 스냅샷 보관) → 이전(2023·2025 전체) → **금액열 8종 SQLite=PostgreSQL 차이 0** (DOUBLE PRECISION 정상). 2025 실적 손익 98,554,802·자본 49,709,177천원 — 합계 148,263,979천원은 기대값과 동일, 구분별 ±1천원은 문서에 기록된 반올림 순서 차이.
- [x] **[Claude]** `dbcore.py`에 `prepare_threshold=None` 추가(풀러 모드 무관 안전) + 문서 10곳의 포트 안내 6543→5432 수정. 테스트 87 passed.
- [ ] **[사용자]** ⚠️ `PG_TEST_URL` 환경변수에 Supabase URL을 넣지 말 것 — 테스트가 표를 TRUNCATE 함.

## 3단계 — Render 배포 (30분, 빌드 5~8분)
- [ ] **[사용자]** render.com 가입(GitHub 연동) → New → Blueprint → 저장소 선택 → `render.yaml` 인식 → Apply.
- [ ] **[사용자]** Environment에 `APP_PASSWORD`(팀 비밀번호), `DATABASE_URL`(Supabase) 입력. `SECRET_KEY`는 자동 생성.
- [ ] **[Claude/사용자]** 빌드 로그 확인(Caddy 다운로드·pip). 실패 시 로그를 공유하면 Dockerfile 수정.
- [ ] **[Claude]** `https://<서비스>.onrender.com/healthz` → `{"ok":true,"auth":true,"db":"postgresql"}` · `/app/` 로그인 · `/forecast` 기본인증 · `/`(소개) 확인.

## 4단계 — 팀 개통 (당일)
- [ ] **[사용자]** 팀원에게 주소·팀 비밀번호(비밀 메모)·작업자 이름 규칙(실명)·첫 접속 30~60초 지연 안내. `docs/팀원_실행가이드.md` 공유.
- [ ] **[동료]** forecast_app에서 「사업 실적 연결」에 2023·2025 결과 JSON 업로드(Render 무료 디스크는 재배포 시 초기화 → 그때마다 다시 올림).
- [ ] **[동료/사용자]** v2 학습 CSV를 budget_app 설정 탭 「학습 CSV 가져오기」로 DB에 적재 → 관리자 [재학습] → `py scripts/v2_model_sync.py --pull`.

## 5단계 — 사내 서버 이관 (일정 확정 후)
- [ ] **[사용자]** 사내 정보보호 규정 확인(외부 클라우드 실데이터 임시 보관) — 검토 종료 시 Supabase 표 비우기/프로젝트 삭제.
- [ ] **[사용자]** 서버 확보(Docker 가능 여부). 인터넷 없는 서버면 이미지 `docker save/load` 반입.
- [ ] **[Claude]** `deploy/docker-compose.yml`로 기동, Supabase → 사내 PG `pg_dump`/`psql` 이전, `.env` 재작성, 개인 계정 전환 착수.

## 자주 막히는 곳
- Render 첫 접속 느림 = 슬립 해제(정상). `/healthz`로 깨울 수 있다.
- v2 JSON 업로드 거부 = 연도 불일치/다연도. budget_app에서 해당 연도로 다시 내보내기.
- PostgreSQL 금액이 SQLite와 다르면 표가 `REAL`로 만들어진 것 — `db_check.py`가 만든 표(`DOUBLE PRECISION`)인지 확인.
