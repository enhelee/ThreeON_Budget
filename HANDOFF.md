# 계획본 생성 프로그램 (1단계) — Handoff

작성일: 2026-07-22 · 상태: **완료** (Task 0~11, 1단계 테스트 21개)

> **⚠ 이 문서는 1단계(계획본 생성) 전용 상세 인수인계이고, 기술 스택 설명은 Streamlit 시절 기준이다.**
> 현재 앱은 **FastAPI 웹 시스템**(`분석프로그램_실행.bat` → http://localhost:8010)이며 전체 테스트는
> **68 passed**(2026-08-04, rev12). 실행법·구조·API·주의사항은 반드시
> **[`../통합_인수인계_MASTER.md`](../통합_인수인계_MASTER.md)(진입점)** 를 먼저 볼 것.
> 그 밖에 루트 [`../HANDOFF.md`](../HANDOFF.md) · [`../변경이력_CHANGELOG.md`](../변경이력_CHANGELOG.md) ·
> [`../연계규격_INTEGRATION.md`](../연계규격_INTEGRATION.md) · [`../PRD_V1.html`](../PRD_V1.html) 참조.

---

## 1. 한눈에

"사업별 예산" 엑셀을 업로드하면, 웹에서 구성한 **지사(열) × 예산과목(행)** 기준으로
`{연도}년 {손익|자본}예산_계획.xlsx` 를 자동 생성하는 로컬 Streamlit 웹앱.

- 위치: `C:\Users\User\Desktop\중장기 예산 소요 전망\budget_app` (자체 git repo, master)
- 스택: Python 3.14 · pandas 3.0.3 · openpyxl 3.1.5 · streamlit 1.58.0 · pytest 9.1.1 (모두 설치됨)
- 실행: `분석프로그램_실행.bat` 더블클릭, 또는 폴더에서 `py -m streamlit run app.py`
- 테스트: 폴더에서 `py -m pytest -q` → **21 passed**

---

## 2. 사용 흐름 (웹 UI)

1. **① 구분·연도** — 손익/자본, 대상연도 선택
2. **② 지사 구성** — 표 편집(그룹 지정·포함 체크·행 추가) → `지사 구성 적용 완료` 저장
3. **③ 예산과목 구성** — 표 편집(대분류·심의대상·포함) → `예산과목 구성 적용 완료` 저장
4. **④ 파일 업로드** — 템플릿 다운로드 가능. 사업별 예산 `.xlsx` 업로드
5. **⑤ 실행** — 결과 파일 다운로드. 미분류/결측 있으면 경고 표시

구성(②③)은 `config/*.json`에 연도별(과목은 손익/자본별)로 저장되어 다음 실행 때 자동 로드됨.

---

## 3. 산출물 구조

`{연도}년 {손익|자본}예산_계획.xlsx`:
- **양식1(월별)** 데이터시트 — 업로드 원본 10개 컬럼 + K열 `검증`(결측 메모) + L열 `심의플래그`("O"/""). 결측 필드 셀은 분홍 강조.
- **종합표** — 동적 레이아웃. A=대분류(세로병합), B=예산과목, C=심의구분, D열~그룹별 지사+소계, 마지막=합계. 셀은 **live SUMIFS 수식**(양식1 시트를 참조 → 엑셀에서 열면 실시간 계산).
- **결측치검토** — 결측/미분류가 있을 때만 생성.

심의대상 과목은 종합표에서 `5천만원 이상(심의대상)` / `5천만원 미만` 2행으로 분리되고,
SUMIFS가 L열 플래그(`"O"` / `"<>O"`)로 나눠 집계.

---

## 4. 모듈 (`src/budget/`)

| 파일 | 책임 |
|---|---|
| `config_store.py` | 지사/과목 구성 JSON 로드·저장·시드. `DEPT_GROUPS`, `SIMUI_THRESHOLD_THOUSAND=50000`, `SEED_ITEMS` |
| `org_structure.py` | config → 종합표 열/행 그룹 레이아웃 |
| `loaders.py` | 사업별 예산 로드. `PLAN_COLUMNS`(위치 1~10), 시트 `양식1(월별)`, 헤더 3행/데이터 4행~ |
| `missing.py` | 필수필드(처지사·예산과목·연예산·사업명) 결측 탐지 |
| `classify.py` | 손익/자본 필터 + 미분류 과목 |
| `plan_summary.py` | (예산과목,심의구분)×처지사 집계 (파이썬 검증용; 실산출은 SUMIFS 사용) |
| `excel_plan_writer.py` | 데이터시트 + 동적 종합표 + SUMIFS + 결측치검토 + `write_plan_workbook` |
| `pipeline_plan.py` | `run_plan(...)` 오케스트레이션 (UI 비의존) |
| `app.py` | Streamlit UI |

핵심 상수/규칙:
- 단위 **천원**, 심의 임계값 **≥50,000천원**
- 지사그룹 고정순서 `["본사","중대형CHP","소형CHP","DH"]`, 대분류는 config 등장순서
- 손익 시드 8개(심의대상 = `수선유지비-건물/구축물`, `수선유지비-열원보완개선및기타`), 자본 시드 11개
- 연예산 결측은 `None` 유지(0과 구분)

---

## 5. 실데이터 검증 (E2E)

파일 `양식1_월별_템플릿(25년 계획분)_R1.xlsx` (프로젝트 루트, 1,700 유효행):

| 구분 | 행수 | 총액(천원) | 미분류 | 결측 |
|---|---|---|---|---|
| 손익 | 1,400 | 103,384,950 | 0 | 0 |
| 자본 | 300 | 69,867,520 | 0 | 0 |

- 시드 19개 과목이 실데이터를 완전 커버 → 미분류 0
- 모든 처지사가 시드 지사목록에 포함 → **종합표 SUMIFS 총액 = 데이터시트 총액** (차이 0, 손익/자본 모두)
- `tests/test_e2e_real.py` 로 회귀 고정 (실파일 없으면 자동 skip)

---

## 6. 계획 대비 변경점 (둘 다 플랜 자체의 오류를 바로잡음)

1. **pandas 3.0 NaN 가드 추가** — 설치된 pandas 3.0은 기본 문자열 dtype에서 `None`을 `NaN`으로 변환.
   플랜 원본의 `v is None` 체크가 무력화되어, `classify` / `plan_summary` / `excel_plan_writer`에
   `pd.isna(...)` 가드를 넣음. 없으면 미분류 목록에 `'nan'`이 끼고 결측금액이 합산됨.
2. **Task 9 테스트 기대값 수정** (`1행/30000` → `2행/90000`) — 테스트 데이터의 두 행 모두 손익 시드
   과목이라 필터 결과는 2행이 맞음. 플랜 기대값이 시드와 불일치한 오기였음.

---

## 7. 알아둘 점 / 주의

- `config/`, `output/`은 `.gitignore` 처리됨(런타임 생성물).
- 로더는 **컬럼명이 아니라 위치(1~10)** 로 읽음. 업로드 파일의 컬럼 순서가 바뀌면 매칭이 틀어짐.
- 종합표 값은 **수식**이라 openpyxl로는 계산되지 않음 — 실제 숫자는 Excel/LibreOffice에서 열어야 보임.
- 처지사·예산과목이 시드/구성에 없으면 종합표에서 조용히 누락됨 → 실행 후 경고(미분류)와 결측치검토 시트를 반드시 확인.

---

## 8. 다음 단계 후보

- **2단계(실적 분석)**: ✅ **구현 완료** — ERP(zrfm2) 전표–사업 매칭 → 계획대비실적·실적 종합표·zrfm2_V1·matched CSV. 실적 탭에 지사·예산과목 구성 선택 + 미반영 표기. 상세는 루트 문서 참조.
- 부서코드→처지사 기준데이터 매칭(현재는 지사명 텍스트+정규화 사용, 부서코드 마스터는 로드만).
- 종합표 수식 결과를 파이썬(`plan_summary.aggregate`)으로 교차검증하는 자동 테스트 추가.
- 3·4단계(예산 표준화·중장기 예측): 설계 단계.

---

## 9. 참고 문서

- 설계: `docs/superpowers/specs/2026-07-22-계획본-design.md`
- 계획: `docs/superpowers/plans/2026-07-22-계획본.md`
- git 로그: `git -C budget_app log --oneline` (Task별 커밋 14개)
